# Append-only log: cho phép git tự union khi 2 nhánh cùng append row mới.
# Run ID format `run-<YYYYMMDD>-<HHMMSS>-<user>` đảm bảo không trùng ID khi merge.
docs/qc-lead/agent-work-log.md merge=union
