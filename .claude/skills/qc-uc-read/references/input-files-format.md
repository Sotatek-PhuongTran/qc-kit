# Định dạng file input requirement — file per-project (qc-uc-read tự cập nhật)

> **Trạng thái khớp thực tế:** _(rỗng — chưa check)_
>
> Giá trị hợp lệ của dòng cờ trên: **rỗng** (chưa check) / `Cần check lại` / `Đã khớp — <YYYY-MM-DD> (qc-uc-read xác nhận)`.
> **User CHỈ sửa dòng cờ này** (đặt `Cần check lại` khi BA đổi format tài liệu); nội dung file do `qc-uc-read` tự cập nhật qua FORMAT CHECK GATE (`workflows/first-audit/1-synthesize-understanding.md` Phase 1 Step 1.4). Đây là file kit-internal — đọc theo path trực tiếp, không qua `path-registry.md`.

---

## 1. Layout thư mục requirement

_(qc-uc-read tự điền từ path-registry nhóm A — `requirement-files` / `requirement-common-files` / `High-level-files` — với path + pattern THẬT trên đĩa của dự án.)_

| Nhóm registry | Path thật | Pattern file thật | Ghi chú |
|---|---|---|---|
| _(chưa điền)_ | | | |

## 2. Các loại file input và cách nhận diện

_(qc-uc-read tự điền: mỗi loại input của dự án — UC doc, FRD, common rules, wireframe/design nếu có — một mục: cách nhận diện file (tên/vị trí/cấu trúc đặc trưng) + vai trò khi audit.)_

_(chưa điền)_

## 3. Bảng prefix mã tham chiếu của dự án

_(qc-uc-read tự điền từ tài liệu THẬT của dự án — vd `CR-xx`, `BR-xx`, `EF-xx`, `SCR-xx`, `AC-xx` — không bịa prefix. Bảng này phục vụ nhận diện reference ở Step 1.5 và grounding Sub-agent A ở Step 1.6.)_

| Prefix | Ý nghĩa | Định nghĩa tại |
|---|---|---|
| _(chưa điền)_ | | |

## 4. Ghi chú đặc thù

_(qc-uc-read tự điền: file bổ trợ, đa ngôn ngữ, quy ước riêng của dự án… Không có gì đặc thù → ghi `Không có`.)_

_(chưa điền)_

## Update history

| Ngày | Thay đổi (qc-uc-read ghi) |
|---|---|
| _(chưa có)_ | |
