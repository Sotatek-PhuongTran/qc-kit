# Agent Work Log

> **Title:** Agent Work Log — master tổng hợp lịch sử chạy skill
> **Created:** 2026-07-16
> **Author:** QC Kit (skeleton khởi tạo — `qc-get-work-log` là sole writer của phần `## Per-device tables`)
> **Version:** v1

> File master tổng hợp lịch sử chạy skill của toàn team. Nguồn dữ liệu: các file JSONL per-device trong `docs/qc-lead/agent-work-log.local/` (`worklog_<device>.jsonl`) — chạy `/qc-get-work-log` để gom về đây (pure append, dedup theo cặp `(device, start)`). KHÔNG skill nào khác được ghi vào phần `## Per-device tables`; mục `## Legacy` đóng băng, không skill nào đụng vào.

## Legacy (dữ liệu trước 2026-05-19 — đóng băng)

_(chưa có dữ liệu — mục legacy đóng băng; `qc-get-work-log` không bao giờ ghi vào đây)_

## Per-device tables

> Auto-generated by `qc-get-work-log`. Mỗi device một mục `### <device>`, các dòng sort theo Start time tăng dần. Dòng đã ingest không bao giờ bị sửa lại — muốn đổi status của dòng cũ, user sửa tay.

_(chưa có dữ liệu — `qc-get-work-log` sẽ điền)_
