# ĐẶC TẢ YÊU CẦU NGƯỜI SỬ DỤNG

**Tiêu đề:** UC56-57/66-68 — Khai thác tin tức công bố trên Mobile  
**Ngày tạo:** 06/05/2026  
**Phiên bản:** v1.4

| Thuộc tính              | Giá trị                            |
| ------------------------- | ------------------------------------ |
| BA phụ trách            | huyen.dinh2                           |
| Phân hệ                 | Ứng dụng Di động (Mobile App)        |
| Loại chức năng          | Khai thác thông tin đã công bố       |
| Đối tượng thực hiện     | Cá nhân / Tổ chức                    |
| Giao diện               | Màn hình Mobile (Portrait)           |
| Ngày tạo                | 06/05/2026                           |
| Phiên bản               | v1.2                                 |

---

## UC56-57/66-68 — Khai thác tin tức công bố trên Mobile

### 1. Mô tả chức năng

- **Tên chức năng:** Khai thác tin tức công bố trên Mobile
- **Mô tả:** Nhóm chức năng cho phép cá nhân, tổ chức tra cứu và xem chi tiết tin tức đầu tư, chính sách nổi bật, dịch vụ công, câu chuyện thành công và các tin tức chuyên đề khác theo nhiều tiêu chí. Bao gồm danh sách tin nổi bật, tin mới nhất, chi tiết bài viết và công cụ lọc bài viết theo ngày đăng.
- **Phân quyền:** Cá nhân/Tổ chức (đã đăng nhập). Hai nhóm đối tượng có cùng hành vi — không có sự phân biệt.
- **Phạm vi ngoài UC (Exclusions):** UC này KHÔNG bao gồm: chia sẻ bài viết ra ngoài, dịch nội dung bài viết sang ngôn ngữ khác (chỉ lọc bài viết theo ngôn ngữ được chọn), tải xuống bài viết.
- **Truy cập chức năng:** Sidebar → Mục **"Tin tức"** → Màn hình Tin tức duy nhất với Tab Bar phân loại (bao gồm tab "Câu chuyện thành công").
- **Điều kiện tiên quyết (Preconditions):** Thiết bị có kết nối mạng ổn định, người dùng đã đăng nhập vào hệ thống.
- **Điều kiện kết thúc (Postconditions):** Hệ thống hiển thị đầy đủ thông tin danh sách bài viết hoặc hiển thị toàn bộ nội dung chi tiết của bài viết được chọn trên màn hình.
- **Chức năng đáp ứng usecase số:** UC56, UC57, UC66, UC67, UC68 (Phụ lục XIV)

---

### 2. Mô tả giao diện

#### 2.1 Màn hình Danh sách Tin tức (Áp dụng chung)

**Mô tả giao diện:**
Màn hình danh sách bài viết duy nhất với tiêu đề cố định **"Tin tức"** — không thay đổi khi người dùng chọn Tab hay áp dụng bộ lọc. Khung Header màu đỏ đậm gồm Nút Quay lại (nếu đi từ màn khác), Tiêu đề, icon Tìm kiếm và icon Lọc. Bên dưới là thanh Tab phân loại vuốt ngang. Tiếp theo là section "Tin nổi bật" hiển thị thẻ Card lớn nền ảnh và section "Tin tức mới nhất" hiển thị danh sách dạng List dọc.

**Khung Header & Tab phân loại:**

| # | Tên trường | Kiểu trường | Giá trị mặc định | Được sửa | Bắt buộc | Mô tả/Ghi chú |
|---|---|---|---|---|---|---|
| 1 | Nút Quay lại (←) | Button (Icon) | — | — | — | **Quy tắc hành động:**<br>- Tap → Quay về màn hình trước. (Xem CMR-06). Hiển thị nếu điều hướng từ màn hình khác, ẩn nếu đang ở tab chính. |
| 2 | Tiêu đề Header | Label | Theo context | — | — | **Quy tắc hiển thị:**<br>- Nằm giữa header, màu trắng trên nền đỏ đậm.<br>- Tiêu đề cố định **"Tin tức"** — không thay đổi khi người dùng chọn Tab hay áp dụng bộ lọc. |
| 3 | Icon Tìm kiếm | Button (Icon) | — | — | — | **Quy tắc hiển thị:**<br>- Icon kính lúp màu trắng, bên phải header.<br><br>**Quy tắc hành động:**<br>- Tap lần 1 → Mở rộng thanh textbox tìm kiếm ở ngay bên dưới header.<br>- Tap lần 2 (khi thanh search đang mở) → Đóng thanh tìm kiếm và xóa từ khóa hiện tại.<br>- Cuộn màn hình → Thanh tìm kiếm không dính cố định (không sticky), tự ẩn khi cuộn xuống. |
| 4 | Thanh tìm kiếm mở rộng | Textbox (Search) | "Tìm kiếm tin tức..." | x | — | **Quy tắc hiển thị:**<br>- Hiển thị bên dưới header khi tap vào icon kính lúp. Placeholder: "Tìm kiếm tin tức...". (Không có icon clear "X").<br><br>**Quy tắc hành động:**<br>- Nhập từ khóa → Hệ thống tìm kiếm theo tiêu đề bài viết áp dụng cho **toàn bộ các tab** (không chỉ tab hiện tại). Kết quả tìm kiếm hiển thị trên tab "Tất cả" bất kể tab nào đang được chọn. Khi chuyển tab → giữ nguyên keyword tìm kiếm và bộ lọc hiện tại. (Xem CMR-01). |
| 5 | Icon Lọc (Filter) | Button (Icon) | — | — | — | **Quy tắc hiển thị:**<br>- Icon filter (slider) màu trắng, ngoài cùng bên phải header.<br><br>**Quy tắc hành động:**<br>- Tap → Mở Modal popup "Bộ lọc tìm kiếm". |
| 6 | Danh sách Tab phân loại | Tab List (Horizontal Scroll) | "Tất cả" | — | — | **Quy tắc hiển thị:**<br>- Nằm dưới Header, cuộn ngang.<br>- Hiển thị đầy đủ **19 Tab** (Tất cả, Chính sách nổi bật, Chính sách đầu tư, Dịch vụ công, Kinh tế, Văn hóa, Giao thông, Y tế, Lao động, Xã hội, Du lịch, Thể thao, Quốc tế, Chính trị, Thời sự, Giáo dục, Tài chính, **Câu chuyện thành công**, Khác).<br>- Tab đang chọn (Active): Nền đỏ đậm, chữ trắng.<br>- Tab không chọn (Inactive): Nền xám nhạt, chữ đen/xanh đen.<br><br>**Quy tắc hành động:**<br>- Tap → Đổi tab và tải lại danh sách bài viết (Tin nổi bật & Tin mới nhất) thuộc Category tương ứng. (Áp dụng **CMR-18** — Debounce Navigation).<br>- Khi đổi tab: Bộ lọc ngày đăng (nếu đang active) **được giữ nguyên** và tiếp tục áp dụng cho tab mới.<br>- **Đa ngôn ngữ (CMR-17):** Tên 19 Tab (Tất cả, Chính sách nổi bật, Kinh tế...) được dịch sang ngôn ngữ hiển thị tương ứng khi người dùng đổi ngôn ngữ. |

**Section 1 — Tin nổi bật:**

| # | Tên trường | Kiểu trường | Giá trị mặc định | Được sửa | Bắt buộc | Mô tả/Ghi chú |
|---|---|---|---|---|---|---|
| 7 | Danh sách Card Tin nổi bật | Carousel (Image Background) | — | — | — | **Quy tắc hiển thị:**<br>- **Trường hợp ≥ 2 bài nổi bật:** Cuộn ngang (horizontal scroll), tự động chuyển sang tin tiếp theo sau mỗi **5 giây**. Không có indicator dot.<br>- **Trường hợp chỉ 1 bài nổi bật:** Hiển thị 1 card tĩnh, không cho phép cuộn ngang.<br>- Không có lazy load — API trả về bao nhiêu bài được đánh dấu "Tin nổi bật" thì hiển thị tất cả ngay lập tức.<br>- **Quy tắc loại trừ:** Các bài viết đã xuất hiện trong section "Tin nổi bật" sẽ **không được hiển thị lại** trong section "Tin tức mới nhất" bên dưới.<br>- **Trạng thái rỗng:** Nếu không có bài viết nào là "Tin nổi bật", ẩn hoàn toàn section này (kéo section "Tin tức mới nhất" lên trên).<br>- Toàn bộ diện tích thẻ là một vùng có thể tap (Clickable Card).<br>- Nền thẻ là ảnh cover của bài viết, có overlay tối màu để nổi chữ.<br>- Góc trên trái có Tag đỏ "Tin nổi bật".<br>- **Tiêu đề bài viết:** Nằm ở giữa, màu trắng, font đậm to. Hiển thị tối đa 2 dòng, quá 2 dòng có dấu `...` ở cuối (truncate).<br>- **Trích dẫn nội dung:** Dưới tiêu đề, lấy 2 dòng đầu trong bài báo. Hiển thị tối đa 2 dòng, quá 2 dòng có dấu `...` ở cuối.<br>- **Footer:** Icon đồng hồ + Ngày đăng (Định dạng DD/MM/YYYY) & Icon user + Full tên người đăng bài. Tên hiển thị đến max-width màn hình; vượt quá thì truncate với `...` ở cuối.<br><br>**Quy tắc hành động:**<br>- Tap vào bất kỳ vị trí nào trên thẻ → Chuyển tiếp đến Màn hình Chi tiết bài viết. (Áp dụng **CMR-18** — Debounce Navigation). |

**Section 2 — Tin tức mới nhất:**

| # | Tên trường | Kiểu trường | Giá trị mặc định | Được sửa | Bắt buộc | Mô tả/Ghi chú |
|---|---|---|---|---|---|---|
| 8 | Tiêu đề section | Label | "Tin tức mới nhất" | — | — | **Quy tắc hiển thị:**<br>- Font đậm, màu đen/xanh đen, căn trái. Nằm ngay dưới phần Tin nổi bật. |
| 9 | Danh sách bài viết | List Card (Vertical) | — | — | — | **Quy tắc hiển thị:**<br>- Hiển thị danh sách cuộn dọc. Hỗ trợ lazy load / infinite scroll (tải 20 bản ghi/lần).<br>- **Sắp xếp:** Mặc định sắp xếp theo Latest (bài viết mới nhất hiển thị lên đầu).<br>- Toàn bộ diện tích thẻ là vùng có thể tap.<br>- **Cấu trúc mỗi Card:**<br>&emsp;• Bên trái: Ảnh Thumbnail bài viết (hình vuông, bo góc). Nếu API trả **null** hoặc ảnh **không tải được** → Hiển thị **placeholder icon** (nền xám, icon ảnh broken).<br>&emsp;• Bên phải:<br>&emsp;&emsp;- Tag Category (VD: "Kinh tế", "Dịch vụ công" - nền màu nhạt, chữ màu đỏ): text quá dài thì **wrap** (không truncate).<br>&emsp;&emsp;- Tiêu đề bài viết: Font đậm, màu đen, tối đa 2 dòng, quá dài có `...` (truncate).<br>&emsp;&emsp;- Trích dẫn mô tả: Màu xám, tối đa 2 dòng, quá dài có `...` (truncate).<br>&emsp;&emsp;- Dưới cùng: Icon đồng hồ + Ngày đăng (DD/MM/YYYY) & Icon user + Full tên người đăng bài (hiển thị đến max-width; vượt quá thì truncate với `...` ở cuối).<br>- **Trạng thái rỗng (No data):** Nếu API trả về danh sách rỗng → Hiển thị *"Không có dữ liệu."* (theo CMR-14).<br>- **Trạng thái không có kết quả (No result):** Nếu tìm kiếm/lọc không ra kết quả → Hiển thị *"Không tìm thấy kết quả."* (theo CMR-14).<br><br>**Quy tắc hành động:**<br>- Tap vào bất kỳ vị trí nào trên item → Chuyển tiếp đến Màn hình Chi tiết bài viết. (Áp dụng **CMR-18** — Debounce Navigation). |

**Quy tắc ngôn ngữ (Áp dụng toàn bộ module Tin tức — tất cả UC56/57/66/67/68):**
- Khi người dùng chọn ngôn ngữ hiển thị trên ứng dụng → Hệ thống **lọc và hiển thị những bài viết có sẵn bằng ngôn ngữ đó** từ API ở tất cả các màn (danh sách, chi tiết, section Tin tức mới nhất cuối trang chi tiết). Không dịch nội dung bài viết sang ngôn ngữ khác.
- Nếu không có bài viết nào thuộc ngôn ngữ đã chọn → Hiển thị *"Không có dữ liệu."* (theo CMR-14).
- **Đa ngôn ngữ UI (CMR-17):** Tên 19 Tab, label **"Tin tức mới nhất"**, placeholder **"Tìm kiếm tin tức..."** được dịch sang ngôn ngữ tương ứng khi người dùng đổi ngôn ngữ hiển thị.

---

#### 2.2 Modal Bộ lọc tìm kiếm

**Mô tả giao diện:**
Hiển thị ở dạng Bottom Sheet hoặc Dialog khi người dùng tap vào Icon Lọc. Modal chứa tùy chọn lọc theo khoảng thời gian Ngày đăng (Tham khảo CMR-15).

| # | Tên trường | Kiểu trường | Giá trị mặc định | Được sửa | Bắt buộc | Mô tả/Ghi chú |
|---|---|---|---|---|---|---|
| 1 | Tiêu đề & Nút Đóng | Label & Button | "Bộ lọc tìm kiếm" | — | — | **Quy tắc hiển thị:**<br>- Tiêu đề font đậm, căn trái. Nút "X" căn phải.<br><br>**Quy tắc hành động:**<br>- Tap nút "X" hoặc chạm ra nền ngoài modal → Đóng modal mà không áp dụng bộ lọc.<br>- **Nhấn Back vật lý (Android)** khi modal đang mở → **Quay lại màn trước**. |
| 2 | Nhãn "Ngày đăng" | Label | "NGÀY ĐĂNG" | — | — | **Quy tắc hiển thị:**<br>- In hoa, màu xám xanh, căn trái. |
| 3 | Input Chọn ngày đăng | Date Range Picker | "Ngày bắt đầu - Ngày kết thúc" | x | — | **Quy tắc hiển thị:**<br>- Box viền xám nhạt, có icon lịch (calendar) bên phải. Placeholder: "Ngày bắt đầu - Ngày kết thúc". (Không có mũi tên dropdown).<br><br>**Quy tắc hành động:**<br>- Tap → Mở Date Picker để chọn khoảng thời gian (Ngày bắt đầu - Ngày kết thúc). Giá trị hiển thị định dạng DD/MM/YYYY. (Xem CMR-15). |
| 4 | Nút "Nhập lại" | Button (Outline) | — | — | — | **Quy tắc hiển thị:**<br>- Nền trắng, viền xám, icon refresh bên trái chữ.<br><br>**Quy tắc hành động:**<br>- Tap → Xóa giá trị trường Ngày đăng về placeholder ban đầu là "Ngày bắt đầu - Ngày kết thúc", giữ nguyên popup không đóng. |
| 5 | Nút "Áp dụng" | Button (Primary) | — | — | — | **Quy tắc hiển thị:**<br>- Nền đỏ đậm, text trắng.<br><br>**Quy tắc hành động:**<br>- Tap khi đã chọn ngày → Đóng Modal và tải lại danh sách bài viết với tham số lọc thời gian đã chọn.<br>- **Tap khi chỉ chọn 1 ngày (CMR-15):**<br>&emsp;• Chỉ chọn Ngày bắt đầu → Lọc từ ngày đó đến hiện tại.<br>&emsp;• Chỉ chọn Ngày kết thúc → Lọc từ đầu đến ngày đó.<br>- **Tap khi chưa chọn ngày nào** → Đóng modal bình thường, không báo lỗi, không áp dụng bộ lọc. |

---

#### 2.3 Màn hình Chi tiết bài viết

**Mô tả giao diện:**
Màn hình chi tiết chứa toàn bộ nội dung của bài viết. Cuộn dọc từ trên xuống dưới. Header đỏ đậm. Phần thân chứa Ảnh bìa, Thông tin Meta (Tag Category, Tiêu đề, Ngày, Tác giả), Nội dung Rich Text và mục "Tin tức mới nhất" ở cuối bài.

**Khung Header:**

| # | Tên trường | Kiểu trường | Giá trị mặc định | Được sửa | Bắt buộc | Mô tả/Ghi chú |
|---|---|---|---|---|---|---|
| 1 | Nút Quay lại (←) | Button (Icon) | — | — | — | **Quy tắc hành động:**<br>- Tap → Quay lại màn hình trước. (Xem CMR-06) |

**Khung Nội dung:**

| # | Tên trường | Kiểu trường | Giá trị mặc định | Được sửa | Bắt buộc | Mô tả/Ghi chú |
|---|---|---|---|---|---|---|
| 3 | Ảnh cover bài viết | Image (Full Width) | — | — | — | **Quy tắc hiển thị:**<br>- Hiển thị đầu trang, width 100% full màn hình, height auto theo tỷ lệ ảnh.<br>- Nếu API trả về **null** hoặc ảnh **không tải được** → Hiển thị **placeholder icon** (nền xám, icon ảnh broken) thay thế, **không** ẩn block. |
| 4 | Tag phân loại | Chip | — | — | — | **Quy tắc hiển thị:**<br>- Nằm ngay dưới ảnh bìa. Ví dụ: "Kinh tế" (nền nhạt, chữ đỏ). Chỉ rõ bài viết thuộc danh mục nào. |
| 5 | Tiêu đề bài viết | Label (H1) | — | — | — | **Quy tắc hiển thị:**<br>- Font to, in đậm, màu đen. |
| 6 | Thông tin Meta | Label | — | — | — | **Quy tắc hiển thị:**<br>- Cùng dòng: Icon đồng hồ + Ngày đăng (DD/MM/YYYY) & Icon user + Full tên người đăng bài (màu xám).<br>- **Tên người đăng quá dài:** Hiển thị đến max-width màn hình; vượt quá thì truncate với `...` ở cuối. |
| 7 | Nội dung chi tiết | Rich Text / HTML | — | — | — | **Quy tắc hiển thị:**<br>- Render nội dung từ HTML/Rich text của API. Hỗ trợ chữ in đậm, in nghiêng, liên kết, ảnh chèn nội bộ trong bài.<br>- **Ảnh inline lỗi / không tải được:** Hiển thị **placeholder icon** (nền xám, icon ảnh broken) thay thế — không ẩn vùng ảnh.<br>- **Tap vào ảnh:** Không hỗ trợ zoom (ngoài phạm vi UC). |

**Khung Tin tức mới nhất:**

| # | Tên trường | Kiểu trường | Giá trị mặc định | Được sửa | Bắt buộc | Mô tả/Ghi chú |
|---|---|---|---|---|---|---|
| 7 | Tiêu đề section | Label | "Tin tức mới nhất" | — | — | **Quy tắc hiển thị:**<br>- Nằm dưới cùng sau khi kết thúc nội dung bài viết. Font đậm. |
| 8 | Danh sách Tin tức mới nhất | List Card (Vertical) | — | — | — | **Quy tắc hiển thị:**<br>- Hiển thị dạng danh sách cuộn dọc, tương tự màn hình List.<br>- Gợi ý hiển thị 5 bài tin tức mới nhất, loại trừ bài viết đang xem hiện tại.<br>- **Lọc theo ngôn ngữ:** Chỉ hiển thị bài viết có sẵn theo ngôn ngữ đang chọn trên ứng dụng. Không scope theo danh mục Tab.<br>- Toàn bộ diện tích thẻ là vùng có thể tap.<br>- **Cấu trúc mỗi Card:** Tương tự list bài viết (Ảnh Thumbnail trái, thông tin chi tiết phải gồm Tag, Tiêu đề, Trích dẫn, Thời gian, Tác giả).<br>- **Trạng thái rỗng:** Nếu không có tin tức, ẩn hoàn toàn section này.<br><br>**Quy tắc hành động:**<br>- Tap vào bất kỳ vị trí nào trên Card → Hệ thống mở màn hình Chi tiết bài viết mới. |

---

### 3. Mô tả các xử lý của chức năng

#### 3.1 Luồng xem danh sách bài viết

1. Người dùng chọn mục **"Tin tức"** từ Sidebar → Hệ thống mở màn hình Tin tức với Tab Bar phân loại. Tiêu đề Header luôn hiển thị **"Tin tức"** — không thay đổi khi chọn Tab.
2. Hệ thống gọi API danh sách Tab phân loại và mặc định active tab đầu tiên ("Tất cả").
3. Hệ thống gọi API lấy danh sách bài viết theo **toàn bộ các tab** và theo ngôn ngữ hiển thị hiện tại của ứng dụng. Dữ liệu trả về phân tách thành 2 tập: "Tin nổi bật" và "Tin tức mới nhất".
4. Trong lúc tải dữ liệu, màn hình hiển thị **Skeleton Loading** cho cả 2 section.
5. Sau khi tải xong: section "Tin nổi bật" hiển thị carousel ngang; section "Tin tức mới nhất" hiển thị danh sách cuộn dọc, sắp xếp mới nhất lên đầu.
6. Khi kéo tới cuối danh sách "Tin tức mới nhất", hệ thống tự động kích hoạt lazy load để tải thêm 20 bài tiếp theo từ API. (Tham khảo CMR). **Nếu mất mạng trong khi đang lazy load:** Dữ liệu đã tải trước đó được giữ nguyên; hệ thống hiển thị thông báo lỗi mất kết nối theo CMR-07 (kiểm tra lại wifi/mạng).
7. Hỗ trợ **Pull to Refresh** (Xem CMR-13): Kéo xuống để gọi lại toàn bộ API, làm mới cả 2 section.

#### 3.2 Luồng Tìm kiếm & Lọc

1. **Tìm kiếm theo từ khóa:** Người dùng tap vào icon kính lúp, thanh tìm kiếm mở rộng ra dưới Header. Khi gõ từ khóa, hệ thống tự động tìm kiếm gần đúng theo tiêu đề bài viết áp dụng cho **toàn bộ các tab**. Kết quả hiển thị trên tab "Tất cả" bất kể tab nào đang được chọn. Khi chuyển tab → giữ nguyên keyword và bộ lọc. (Tham khảo CMR-01). Tap lại icon kính lúp → Thanh tìm kiếm đóng lại.
2. **Lọc theo bộ lọc:** Người dùng tap vào icon bộ lọc, popup mở lên. Người dùng chọn khoảng ngày đăng và nhấn "Áp dụng". Bộ lọc được áp dụng **xuyên suốt tất cả các Tab** (không reset khi đổi tab). Hệ thống đóng popup và gọi lại API. Khi bấm "Nhập lại", giá trị ngày đăng về rỗng nhưng popup không đóng. Bấm "Áp dụng" khi chỉ chọn 1 ngày → áp dụng theo CMR-15 (xem §2.2 Row #5). Bấm "Áp dụng" khi không chọn ngày nào → đóng modal, không báo lỗi, không áp dụng lọc.

#### 3.3 Luồng xem chi tiết bài viết

1. Tại danh sách (Tin nổi bật, Tin mới nhất), người dùng tap vào vị trí bất kỳ trên Card bài viết → Hệ thống mở màn hình Chi tiết bài viết.
2. Trong khi tải, hiển thị **Skeleton Loading**. Khi API phản hồi, hiển thị đầy đủ: Tag phân loại, Tiêu đề, Ngày đăng, Tác giả và toàn bộ nội dung Rich Text HTML.
3. Người dùng cuộn xuống phần "Tin tức mới nhất" ở cuối màn hình và tap vào một Card → Hệ thống mở màn hình Chi tiết bài viết mới (màn hình khác). Nhấn Quay lại (←) sẽ trở về màn hình Chi tiết trước đó.

#### 3.4 Xử lý lỗi (Tham chiếu CMR-07)

| Tình huống lỗi | Thông báo hiển thị | Hành vi hệ thống |
| --- | --- | --- |
| Lỗi mạng / Mất kết nối | *"Không thể kết nối. Vui lòng kiểm tra mạng và thử lại."* + nút **"Thử lại"** | Giữ nguyên màn hình, hiển thị trạng thái lỗi thay cho nội dung. |
| Lỗi 401 (Session hết hạn) | *"Phiên đăng nhập hết hạn."* (Toast) | Hệ thống tự động sử dụng refresh token để cấp lại access token mới. Nếu refresh token đã hết hạn hoặc không hợp lệ (quá 15 ngày) → chuyển về màn hình Đăng nhập. (Xem CMR-07) |
| Lỗi API (HTTP 500) | *"Hệ thống đang bận. Vui lòng thử lại sau."* | Giữ nguyên màn hình, chỉ hiển thị thông báo. |
| Không tìm thấy bài viết (HTTP 404) | *"Nội dung không tồn tại hoặc đã bị xóa."* | Áp dụng khi người dùng mở màn chi tiết của bài viết đã bị xóa từ CMS. Xử lý theo CMR-07: hiển thị thông báo và quay lại màn hình danh sách trước đó. |
| Offline / Không có mạng khi mở app | — | Xử lý theo CMR-07. |
| Partial failure (API "Tin nổi bật" thành công nhưng API "Tin tức mới nhất" lỗi, hoặc ngược lại) | — | Hiển thị section tải thành công bình thường. Section bị lỗi hiển thị thông báo lỗi riêng theo CMR-07 — không block toàn bộ màn hình. |
| Tìm kiếm / Lọc không có kết quả | — | Hiển thị *"Không tìm thấy kết quả."* (CMR-14 — No result). |

#### 3.5 Tiêu chí chấp nhận (Acceptance Criteria)

- **AC1:** Mục "Tin tức" từ Sidebar mở đúng 1 màn hình Tin tức duy nhất. Tiêu đề Header luôn là **"Tin tức"** — không thay đổi khi chọn Tab hay áp dụng bộ lọc. Tab Bar hiển thị đầy đủ 19 Tab (bao gồm "Câu chuyện thành công"), đổi tab thì danh sách tải lại đúng danh mục.
- **AC2:** Chọn ngôn ngữ khác → toàn bộ danh sách bài viết (màn danh sách + section Tin tức mới nhất trong màn chi tiết) được lọc lại theo ngôn ngữ đó. Nội dung bài không bị dịch.
- **AC3:** Icon search mở rộng thanh nhập liệu, tìm kiếm gần đúng theo tiêu đề áp dụng toàn bộ các tab, kết quả hiển thị trên tab "Tất cả". Tap lại icon search → đóng thanh. Cuộn màn hình → thanh search ẩn (không sticky).
- **AC4:** Bộ lọc ngày đăng áp dụng xuyên suốt tất cả Tab (không reset khi đổi tab). Cho phép chọn chỉ 1 ngày theo CMR-15. Bấm "Áp dụng" khi không chọn ngày nào → đóng modal, không báo lỗi.
- **AC5:** Bài viết đã xuất hiện trong section "Tin nổi bật" không được xuất hiện lại trong section "Tin tức mới nhất".
- **AC6:** Thẻ Card bài viết (Tin nổi bật, Tin mới nhất) chứa đầy đủ Tag, Title, Trích dẫn, Date (DD/MM/YYYY) và Full tên người đăng bài. Title và Trích dẫn giới hạn đúng 2 dòng và có ellipsis `...` khi vượt quá.
- **AC7:** Card bài viết có thể được bấm tại bất kỳ vị trí nào để mở chi tiết.
- **AC8:** "Tin nổi bật" ẩn hoàn toàn section khi API không trả về bài nào có flag "Tin nổi bật".
- **AC9:** Skeleton Loading hiển thị trong thời gian chờ API phản hồi ở cả màn danh sách lẫn màn chi tiết.
- **AC10:** Pull to Refresh hoạt động đúng — kéo xuống làm mới toàn bộ dữ liệu từ API.
- **AC11:** Tìm kiếm không có kết quả → hiển thị *"Không tìm thấy kết quả."* (CMR-14). Lọc API trả rỗng → hiển thị *"Không có dữ liệu."* (CMR-14).

---

## 4. Lịch sử cập nhật

| Ngày | Phiên bản | Mục cập nhật | Before | After | Ghi chú |
| --- | --- | --- | --- | --- | --- |
| 2026-05-08 | v1 → v1.1 | 1. Truy cập chức năng | Sidebar → 3 mục riêng biệt → 3 màn hình riêng | Sidebar → 1 mục "Tin tức" → 1 màn hình duy nhất | Gộp UC67, UC68 vào 1 màn |
| 2026-05-08 | v1 → v1.1 | 2.1 Row #2 — Tiêu đề Header | Đổi theo màn: "Tin tức" / "Dịch vụ công" / "Câu chuyện thành công" | Luôn cố định "Tin tức", không đổi khi chọn Tab | Heading cố định |
| 2026-05-08 | v1 → v1.1 | 2.1 Row #6 — Tab Bar | 18 Tab (màn Tin tức); Ẩn Tab Bar (màn DVC & CCTC) | 19 Tab — bổ sung "Câu chuyện thành công", áp dụng cho 1 màn duy nhất | Thêm tab CCTC |
| 2026-05-08 | v1 → v1.1 | 3.1 Luồng bước 1 | Chọn 1 trong 3 mục Sidebar | Chọn mục "Tin tức" từ Sidebar | Đồng bộ với cấu trúc mới |
| 2026-05-08 | v1 → v1.1 | 3.5 AC1 | 3 mục / 3 màn / 18 Tab | 1 mục / 1 màn / 19 Tab / Heading cố định | Đồng bộ với cấu trúc mới |
| 2026-05-11 | v1.1 → v1.2 | §2.2 Row #5 — Validation bộ lọc ngày | Bấm "Áp dụng" khi chưa chọn ngày → lỗi "Trường bắt buộc" | Apply CMR-15: cho phép 1 ngày; không chọn ngày nào → đóng modal, không lỗi | BA Q&A |
| 2026-05-11 | v1.1 → v1.2 | §2.3 Row #7 — Ảnh inline lỗi | Không có rule | Hiển thị placeholder icon khi ảnh inline không tải; không zoom khi tap | BA Q&A + wireframe |
| 2026-05-11 | v1.1 → v1.2 | §2.3 Row #3 — Ảnh cover null | Nếu null → ẩn block | Nếu null hoặc không tải được → hiển thị placeholder icon, không ẩn block | BA Q&A + wireframe |
| 2026-05-11 | v1.1 → v1.2 | §2.1 Row #9 — Thumbnail null | Không có rule | Nếu null hoặc không tải được → hiển thị placeholder icon | BA Q&A + wireframe |
| 2026-05-11 | v1.1 → v1.2 | §2.1 Row #7/9 + §2.3 Row #6 — Tên người đăng dài | Không có rule | Hiển thị đến max-width; vượt quá → truncate `...` | BA Q&A |
| 2026-05-11 | v1.1 → v1.2 | §2.1 Row #9 — Tag Category dài | Không có rule | Text quá dài → wrap (không truncate) | BA Q&A |
| 2026-05-11 | v1.1 → v1.2 | §2.1 Row #7 — Carousel 1 bài | Không có rule | 1 bài: hiển thị tĩnh, không cuộn; ≥20 bài: tự chuyển 5s + cuộn ngang; không có dot indicator | BA Q&A |
| 2026-05-11 | v1.1 → v1.2 | §3.4 — Partial failure | Không có rule | Hiển thị section thành công; section lỗi báo lỗi riêng theo CMR-07 | BA Q&A |
| 2026-05-11 | v1.1 → v1.2 | §3.1 bước 6 — Lazy load mất mạng | Không có rule | Giữ data cũ; show lỗi mất kết nối theo CMR-07 | BA Q&A |
| 2026-05-11 | v1.1 → v1.2 | §2.1 Row #6 + Card — CMR-18 | Không áp dụng | Áp dụng CMR-18 (Debounce Navigation) cho tap card và đổi tab | BA Q&A |
| 2026-05-11 | v1.1 → v1.2 | §2.2 Row #1 — Back vật lý Android | Không có rule | Nhấn Back → Quay lại màn trước | BA Q&A |
| 2026-05-11 | v1.1 → v1.2 | Quy tắc ngôn ngữ — CMR-17 UI labels | Chỉ lọc bài viết theo ngôn ngữ | Bổ sung: tên 19 Tab, label, placeholder cũng được dịch theo CMR-17 | BA Q&A |
| 2026-05-11 | v1.2 → v1.3 | Xử lý lỗi — HTTP 401 | (Không có) | Bổ sung xử lý lỗi 401: auto refresh token, nếu hết hạn >15 ngày → redirect đăng nhập + toast "Phiên đăng nhập hết hạn." | Đồng bộ CMR-07 (B1) |
| 2026-05-12 | v1.3 → v1.4 | Search scope (Tab) | Tìm kiếm trong phạm vi Tab đang active | Tìm kiếm áp dụng toàn bộ tab, kết quả hiển thị trên tab "Tất cả". Chuyển tab giữ nguyên keyword và bộ lọc | Đồng bộ CMR-01 v1.4 |
