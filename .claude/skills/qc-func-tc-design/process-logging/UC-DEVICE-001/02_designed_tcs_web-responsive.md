# Test Cases — UC-DEVICE-001 Gán thiết bị cho bệnh nhân — web-responsive

**Total test cases:** 95 (GUI: 46, FUNC: 49)
**Platform variant:** web-responsive
**Source UC:** UC-DEVICE-001_gan-thiet-bi-cho-benh-nhan_audited_20260604_v2.md
**Source scenarios (if any):** (không có — chưa thiết kế bằng qc-func-scenario-design)
**Output language:** VI

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Mở modal từ chi tiết hồ sơ bệnh nhân (variant patient-first) | TC_001, TC_047 | Covered |
| AC-02 | Mở modal từ danh sách thiết bị (variant device-first) | TC_002, TC_048 | Covered |
| AC-03 | Selector trong trạng thái loading | TC_003 | Covered |
| AC-04 | Pool tenant không có thiết bị Sẵn sàng (variant patient-first) | TC_004, TC_005, TC_026, TC_080 | Covered |
| AC-05 | Bệnh nhân `archived` ở variant patient-first (Ex 2x) | TC_006, TC_019, TC_070 | Covered |
| AC-06 | User chưa chọn item nhưng cố submit | TC_067, TC_068, TC_069 | Covered |
| AC-07 | Gán thành công (hồ sơ có SOS) | TC_027, TC_049, TC_050, TC_055, TC_091 | Covered |
| AC-08 | Gán thành công, hồ sơ chưa có SOS (Alt 6a) | TC_051, TC_056, TC_092 | Covered |
| AC-09 | Gán thành công — verify integration | TC_052, TC_053, TC_054, TC_087, TC_088, TC_089 | Covered |
| AC-10 | Race condition — thiết bị bị gán đồng thời (Ex 5x-a) | TC_020, TC_071, TC_095 | Covered |
| AC-11 | Race condition — bệnh nhân bị gán đồng thời (Ex 5x-b) | TC_021, TC_072 | Covered |
| AC-12 | System error khi lưu liên kết hoặc audit log (Ex 5x-c) | TC_022, TC_073, TC_074 | Covered |
| AC-13 | SOS sync fail sau khi liên kết đã commit (Ex 6x) | TC_075, TC_076 | Covered |
| AC-14 | Cross-tenant attempt — server-side validation | TC_077 | Covered |
| AC-15 | Phiên hết hạn giữa flow (Ex 4y) — CRULE-013 | TC_028, TC_078, TC_079 | Covered |
| AC-16 | Đóng modal qua 3 kênh (CRULE-007) | TC_029, TC_030, TC_031, TC_083 | Covered |
| AC-17 | Chặn đóng modal khi đang xử lý | TC_011, TC_084, TC_086 | Covered |
| AC-18 | Hiển thị song ngữ VN/EN toàn modal (CRULE-010) | TC_037, TC_046, TC_090 | Covered |
| AC-19 | Spinner trên nút "Xác nhận gán" lúc submit | TC_007, TC_025 | Covered |
| AC-20 | Server-validation khi cross-tenant — verbatim message | TC_077 | Covered |
| AC-21 | Audit log entry — schema nghiệp vụ | TC_054 | Covered |

---

## I. Màn hình: Modal gán thiết bị cho bệnh nhân (SCR-DEVICE-002)

### I.1. Kiểm tra UI/UX của màn hình: Modal gán thiết bị cho bệnh nhân

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Kiểm tra màn hình khởi tạo modal: variant patient-first | 1. Đăng nhập Org Portal bằng tài khoản Nhân viên tổ chức y tế của tenant có ít nhất 1 thiết bị "Sẵn sàng" và 1 bệnh nhân đang theo dõi chưa có thiết bị.<br>2. Điều hướng đến màn hình chi tiết hồ sơ bệnh nhân của bệnh nhân này. | 1. Nhấp vào hành động "Gán thiết bị" trên hồ sơ bệnh nhân.<br>2. Kiểm tra màn hình khởi tạo modal "Modal gán thiết bị cho bệnh nhân". | 2. Hiển thị modal "Modal gán thiết bị cho bệnh nhân" giống design:<br>- Tiêu đề modal song ngữ: "Gán thiết bị cho bệnh nhân / Assign device to patient".<br>- Variant tag (chip) song ngữ: "Vào từ hồ sơ Bệnh nhân / From patient profile".<br>- Icon "Đóng" (X) ở header (aria-label "Đóng"), enabled.<br>- Context block hiển thị bệnh nhân cố định: mã bệnh nhân — họ tên, "Ngày sinh {dob} · {gender}" với định dạng DD/MM/YYYY (CRULE-001), badge song ngữ "Đang theo dõi / Active".<br>- Selector "Thiết bị / Device" kèm dấu * bắt buộc, placeholder song ngữ "Chọn thiết bị Sẵn sàng… / Select an available device…", helper text song ngữ "Chỉ hiển thị thiết bị đang ở trạng thái Sẵn sàng. / Only devices in Available status are shown.", giá trị mặc định: trống.<br>- Info banner SOS song ngữ luôn hiển thị (title + body verbatim VN+EN).<br>- Footer: nút "Huỷ / Cancel" enabled, nút "Xác nhận gán / Confirm assignment" có icon link leading, disabled mặc định. | P0 |
| TC_002 | Kiểm tra màn hình khởi tạo modal: variant device-first | 1. Đăng nhập Org Portal bằng tài khoản Nhân viên tổ chức y tế của tenant có ít nhất 1 thiết bị "Sẵn sàng" và 1 bệnh nhân đủ điều kiện.<br>2. Điều hướng đến màn hình "Danh sách thiết bị tổ chức" (SCR-DEVICE-001 / UC-DEVICE-003). | 1. Chọn 1 thiết bị đang ở trạng thái "Sẵn sàng" trên danh sách.<br>2. Nhấp vào hành động "Gán" cho thiết bị đó.<br>3. Kiểm tra màn hình khởi tạo modal. | 3. Hiển thị modal "Modal gán thiết bị cho bệnh nhân" giống design:<br>- Tiêu đề modal song ngữ: "Gán thiết bị cho bệnh nhân / Assign device to patient".<br>- Variant tag (chip) song ngữ: "Vào từ danh sách Thiết bị / From device list".<br>- Context block hiển thị thiết bị cố định: mã thiết bị (ví dụ "D000123456"), "IMEI {masked}" chỉ hiện 4 số cuối, badge song ngữ "Sẵn sàng / Available".<br>- Selector "Bệnh nhân / Patient" kèm dấu * bắt buộc, placeholder song ngữ "Chọn bệnh nhân đang theo dõi… / Select an active patient…", helper text song ngữ "Chỉ hiển thị bệnh nhân đang theo dõi và chưa có thiết bị. / Only active patients without an assigned device are shown.", giá trị mặc định: trống.<br>- Info banner SOS song ngữ luôn hiển thị.<br>- Footer: nút "Huỷ / Cancel" enabled, nút "Xác nhận gán / Confirm assignment" disabled mặc định. | P0 |
| TC_003 | Kiểm tra trạng thái loading-candidates của selector | 1. Đăng nhập Org Portal.<br>2. Cấu hình network throttle để API danh sách ứng viên trả về chậm. | 1. Mở modal "Modal gán thiết bị cho bệnh nhân" từ điểm vào bất kỳ.<br>2. Quan sát trạng thái selector trong lúc danh sách đang tải. | 2. Selector hiển thị dòng chờ song ngữ "Đang tải danh sách… / Loading list…"; selector bị disabled (không nhận click); nút "Xác nhận gán / Confirm assignment" disabled. | P1 |
| TC_004 | Kiểm tra trạng thái empty của selector: kho không có thiết bị Sẵn sàng (variant patient-first) | 1. Đăng nhập Org Portal bằng tài khoản tenant không có thiết bị "Sẵn sàng" trong kho.<br>2. Điều hướng đến hồ sơ bệnh nhân đủ điều kiện. | 1. Mở modal qua hành động "Gán thiết bị".<br>2. Quan sát trạng thái selector sau khi tải xong. | 2. Selector hiển thị empty message song ngữ: "Kho thiết bị của tổ chức chưa có thiết bị nào sẵn sàng để gán. / Your organization's device pool has no available devices to assign."; nút "Xác nhận gán / Confirm assignment" disabled. | P1 |
| TC_005 | Kiểm tra trạng thái empty của selector: không có bệnh nhân đủ điều kiện (variant device-first) | 1. Đăng nhập Org Portal bằng tài khoản tenant không có bệnh nhân đủ điều kiện (đang theo dõi và chưa có thiết bị).<br>2. Điều hướng đến danh sách thiết bị, chọn 1 thiết bị "Sẵn sàng". | 1. Nhấp vào hành động "Gán" cho thiết bị đó.<br>2. Quan sát trạng thái selector sau khi tải xong. | 2. Selector hiển thị empty message song ngữ: "Không có bệnh nhân nào đủ điều kiện gán (đang theo dõi và chưa có thiết bị). / No eligible patients to assign (active and without a device)."; nút "Xác nhận gán / Confirm assignment" disabled. | P1 |
| TC_006 | Kiểm tra trạng thái blocked-archived (variant patient-first) | 1. Đăng nhập Org Portal.<br>2. Điều hướng đến hồ sơ bệnh nhân đang ở trạng thái "Đã ngưng theo dõi" (archived). | 1. Nhấp vào hành động "Gán thiết bị".<br>2. Quan sát trạng thái modal sau khi mở. | 2. Modal mở ở trạng thái blocked-archived:<br>- Hiển thị inline banner danger song ngữ ở vùng dưới context block: title "Không thể gán thiết bị cho bệnh nhân đã ngưng theo dõi. / Cannot assign a device to an archived patient." + body "Hãy khôi phục theo dõi trước khi gán lại thiết bị. / Restore monitoring before assigning a device again."<br>- Nút "Xác nhận gán / Confirm assignment" disabled (theo `canConfirm` logic). | P0 |
| TC_007 | Kiểm tra trạng thái submitting của nút Xác nhận | 1. Đăng nhập Org Portal.<br>2. Cấu hình network throttle để API tạo liên kết trả về chậm.<br>3. Mở modal và chọn 1 cặp thiết bị/bệnh nhân hợp lệ. | 1. Nhấp vào nút "Xác nhận gán / Confirm assignment".<br>2. Quan sát trạng thái nút và modal trong lúc đang xử lý. | 2. Nút "Xác nhận gán / Confirm assignment" hiển thị **spinner leading + giữ nguyên nhãn song ngữ "Xác nhận gán / Confirm assignment"** (không thay nhãn bằng text loading riêng); nút disabled trong khi loading; nút "Huỷ / Cancel" disabled; icon "Đóng" (X) disabled; selector disabled; click vào backdrop không đóng modal. | P0 |
| TC_008 | Kiểm tra info banner SOS luôn hiển thị mặc định | 1. Đăng nhập Org Portal.<br>2. Điều hướng đến điểm vào bất kỳ (patient-first hoặc device-first). | 1. Mở modal "Modal gán thiết bị cho bệnh nhân".<br>2. Kiểm tra info banner SOS ở thân modal. | 2. Info banner SOS hiển thị luôn (không có điều kiện ẩn) với title song ngữ "Đồng bộ 3 số SOS xuống thiết bị / Sync the 3 SOS numbers to the device" + body VN "3 số SOS trên hồ sơ bệnh nhân sẽ được đồng bộ xuống thiết bị ngay sau khi gán. Số SOS được quản lý trên hồ sơ bệnh nhân — bạn không cần nhập tại đây." + body EN "The 3 SOS numbers on the patient profile will sync to the device right after assignment. SOS numbers are managed on the patient profile — you don't need to enter them here." | P1 |
| TC_009 | Tiêu đề modal — hiển thị song ngữ | 1. Đăng nhập Org Portal. | 1. Mở modal "Modal gán thiết bị cho bệnh nhân" từ điểm vào bất kỳ.<br>2. Kiểm tra tiêu đề modal ở header. | 2. Hiển thị tiêu đề song ngữ verbatim "Gán thiết bị cho bệnh nhân / Assign device to patient" theo CRULE-010, không bị cắt, không bị xuống dòng giữa cặp song ngữ. | P2 |
| TC_010 | Variant tag (chip) — đổi nhãn theo điểm vào | 1. Đăng nhập Org Portal. | 1. Mở modal từ chi tiết hồ sơ bệnh nhân.<br>2. Kiểm tra nhãn variant tag.<br>3. Đóng modal, điều hướng sang danh sách thiết bị.<br>4. Mở modal bằng hành động "Gán" cho 1 thiết bị "Sẵn sàng".<br>5. Kiểm tra nhãn variant tag. | 2. Hiển thị nhãn song ngữ verbatim "Vào từ hồ sơ Bệnh nhân / From patient profile".<br>5. Hiển thị nhãn song ngữ verbatim "Vào từ danh sách Thiết bị / From device list". | P2 |
| TC_011 | Nút Đóng (X) — kiểm tra hiển thị và trạng thái | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Kiểm tra hiển thị icon X ở header.<br>2. Đưa modal vào state submitting (chọn cặp hợp lệ + bấm Xác nhận, network chậm). | 1. Icon "Đóng" (X) hiển thị ở góc phải header, kích thước 16px, có aria-label "Đóng", trạng thái enabled.<br>2. Khi state=submitting, icon "Đóng" (X) chuyển sang disabled (không nhận click). | P1 |
| TC_012 | Context block variant device-first — hiển thị thông tin thiết bị | 1. Đăng nhập Org Portal.<br>2. Mở modal qua danh sách thiết bị (chọn thiết bị "Sẵn sàng"). | 1. Kiểm tra context block trong modal. | 1. Context block hiển thị thiết bị fixed read-only:<br>- Mã thiết bị (ví dụ "D000123456").<br>- "IMEI {masked}" che các ký tự đầu, chỉ hiển thị 4 số cuối (qua DEVICE_DATA.maskImei()).<br>- Badge trạng thái song ngữ verbatim "Sẵn sàng / Available".<br>Context block không nhận thao tác chỉnh sửa. | P1 |
| TC_013 | Context block variant patient-first — hiển thị thông tin bệnh nhân | 1. Đăng nhập Org Portal.<br>2. Mở modal qua chi tiết hồ sơ bệnh nhân. | 1. Kiểm tra context block trong modal. | 1. Context block hiển thị bệnh nhân fixed read-only:<br>- "{mã bệnh nhân} — {họ tên}".<br>- "Ngày sinh {dob} · {gender}" với dob ở định dạng DD/MM/YYYY (CRULE-001).<br>- Badge trạng thái song ngữ verbatim "Đang theo dõi / Active".<br>Context block không nhận thao tác chỉnh sửa. | P1 |
| TC_014 | Selector — nhãn, dấu * bắt buộc và placeholder song ngữ | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Kiểm tra hiển thị selector ở variant patient-first.<br>2. Đổi sang variant device-first, kiểm tra selector. | 1. Selector hiển thị nhãn song ngữ "Thiết bị / Device" có dấu * (CRULE-005) chỉ trường bắt buộc; placeholder song ngữ "Chọn thiết bị Sẵn sàng… / Select an available device…".<br>2. Selector hiển thị nhãn song ngữ "Bệnh nhân / Patient" có dấu *; placeholder song ngữ "Chọn bệnh nhân đang theo dõi… / Select an active patient…". | P1 |
| TC_015 | Selector — helper text dưới selector | 1. Đăng nhập Org Portal.<br>2. Mở modal. | 1. Kiểm tra helper text dưới selector ở variant patient-first.<br>2. Đổi variant device-first, kiểm tra helper text. | 1. Helper text song ngữ verbatim: "Chỉ hiển thị thiết bị đang ở trạng thái Sẵn sàng. / Only devices in Available status are shown.", luôn hiển thị.<br>2. Helper text song ngữ verbatim: "Chỉ hiển thị bệnh nhân đang theo dõi và chưa có thiết bị. / Only active patients without an assigned device are shown.", luôn hiển thị. | P2 |
| TC_016 | Selector — mở dropdown khi click | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default, selector đã ready (không loading, không empty). | 1. Nhấp vào selector. | 1. Dropdown mở ra, hiển thị danh sách ứng viên (thiết bị "Sẵn sàng" hoặc bệnh nhân đủ điều kiện) của tenant; mỗi item hiển thị thông tin nhận diện (mã + tên / IMEI cuối); cho phép cuộn nếu danh sách dài. | P2 |
| TC_017 | Selector — chọn 1 item và lưu vào state nội bộ | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Mở dropdown selector.<br>2. Chọn 1 item bất kỳ. | 2. Item được chọn hiển thị trong selector; dropdown đóng lại; nút "Xác nhận gán / Confirm assignment" chuyển từ disabled sang enabled (vì đã đủ `canConfirm`). | P1 |
| TC_018 | Selector — no-match message khi search không khớp | 1. Đăng nhập Org Portal.<br>2. Mở modal, selector đã ready, danh sách có ít nhất 1 item. | 1. Nhấp vào selector.<br>2. Gõ chuỗi không khớp với bất kỳ item nào trong danh sách (ví dụ "ZZZZ___"). | 2. Dropdown hiển thị inline message song ngữ verbatim "Không tìm thấy / No results"; nút "Xác nhận gán / Confirm assignment" vẫn giữ trạng thái hiện tại (disabled nếu chưa chọn item nào trước đó); cho phép xoá chuỗi search để xem lại danh sách. | P2 |
| TC_019 | Banner danger #3.1 (archived) — verbatim song ngữ | 1. Đăng nhập Org Portal.<br>2. Điều hướng đến hồ sơ bệnh nhân đang ở trạng thái "Đã ngưng theo dõi" (archived). | 1. Mở modal qua hành động "Gán thiết bị".<br>2. Kiểm tra inline banner danger trong modal. | 2. Banner danger hiển thị verbatim song ngữ:<br>- Title: "Không thể gán thiết bị cho bệnh nhân đã ngưng theo dõi. / Cannot assign a device to an archived patient."<br>- Body: "Hãy khôi phục theo dõi trước khi gán lại thiết bị. / Restore monitoring before assigning a device again."<br>Banner không có nút action; nút "Xác nhận gán / Confirm assignment" disabled. | P0 |
| TC_020 | Banner danger #3.2 (concurrent-device) — verbatim song ngữ | 1. Chuẩn bị trạng thái race: dùng tài khoản User B cùng tenant gán thiết bị D1 cho bệnh nhân P2 thành công (ngoài luồng).<br>2. Tài khoản User A đang mở modal variant patient-first và đã chọn thiết bị D1 trước khi User B kịp gán. | 1. User A nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát banner danger trả về sau khi server từ chối. | 2. Banner danger hiển thị verbatim song ngữ:<br>- Title: "Thiết bị vừa được gán cho bệnh nhân khác. / This device was just assigned to another patient."<br>- Body: "Vui lòng chọn thiết bị khác trong kho. / Please choose another device from the pool."<br>Selector cho phép chọn lại thiết bị khác; trạng thái thiết bị D1 không thay đổi do thao tác của User A. | P0 |
| TC_021 | Banner danger #3.3 (concurrent-patient) — verbatim song ngữ (giữ "liên kết"/"link") | 1. Chuẩn bị trạng thái race: dùng User B cùng tenant gán thiết bị khác cho bệnh nhân P1 thành công.<br>2. User A đang mở modal variant device-first và đã chọn bệnh nhân P1 trước. | 1. User A nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát banner danger trả về sau khi server từ chối. | 2. Banner danger hiển thị verbatim song ngữ (Q1 Resolved — giữ "liên kết"/"link"):<br>- Title: "Bệnh nhân vừa được gán một thiết bị khác. / This patient was just assigned another device."<br>- Body: "Vui lòng kiểm tra lại — hệ thống không tạo liên kết mới. / Please review — no new link was created."<br>Không tạo liên kết mới ở thao tác của User A. | P0 |
| TC_022 | Banner danger #3.4 (system error) — verbatim song ngữ | 1. Đăng nhập Org Portal.<br>2. Mock server trả lỗi 5xx khi lưu liên kết hoặc khi ghi audit log.<br>3. Mở modal và chọn cặp hợp lệ. | 1. Nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát banner danger sau khi server lỗi. | 2. Banner danger hiển thị verbatim song ngữ:<br>- Title: "Không thể gán thiết bị do lỗi hệ thống. / Cannot assign the device due to a system error."<br>- Body: "Vui lòng thử lại. Nếu lỗi tiếp tục, liên hệ Sotatek Support. / Please try again. If the error persists, contact Sotatek Support."<br>Modal không đóng; trạng thái thiết bị/hồ sơ bệnh nhân/audit log giữ nguyên (rollback toàn bộ — không tạo liên kết một phần). | P0 |
| TC_023 | Info banner SOS — title + body song ngữ verbatim | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Kiểm tra info banner SOS ở thân modal. | 1. Banner info hiển thị verbatim:<br>- Title song ngữ: "Đồng bộ 3 số SOS xuống thiết bị / Sync the 3 SOS numbers to the device".<br>- Body VN: "3 số SOS trên hồ sơ bệnh nhân sẽ được đồng bộ xuống thiết bị ngay sau khi gán. Số SOS được quản lý trên hồ sơ bệnh nhân — bạn không cần nhập tại đây."<br>- Body EN: "The 3 SOS numbers on the patient profile will sync to the device right after assignment. SOS numbers are managed on the patient profile — you don't need to enter them here."<br>Banner không bị cắt, không truncate cặp song ngữ. | P1 |
| TC_024 | Nút Huỷ — hiển thị nhãn song ngữ và trạng thái mặc định | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Kiểm tra nút footer trái. | 1. Nút secondary (ghost) hiển thị nhãn song ngữ verbatim "Huỷ / Cancel", trạng thái enabled mặc định. | P2 |
| TC_025 | Nút Xác nhận gán — hiển thị nhãn song ngữ + icon leading + disabled mặc định | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default (chưa chọn item). | 1. Kiểm tra nút footer phải. | 1. Nút primary hiển thị icon "link" leading + nhãn song ngữ verbatim "Xác nhận gán / Confirm assignment"; trạng thái disabled vì `canConfirm = false` (chưa chọn item). | P1 |
| TC_026 | Tooltip nút Xác nhận khi disabled do empty-candidates | 1. Đăng nhập Org Portal bằng tài khoản tenant ở trạng thái không có ứng viên đủ điều kiện.<br>2. Mở modal. | 1. Di chuột qua (hover) nút "Xác nhận gán / Confirm assignment" disabled. | 1. Tooltip hiển thị song ngữ verbatim tuỳ pickerKind:<br>- Variant patient-first: "Chưa có thiết bị Sẵn sàng nào để gán. / No available device to assign yet."<br>- Variant device-first: "Chưa có bệnh nhân đủ điều kiện để gán. / No eligible patient to assign yet." | P2 |
| TC_027 | Toast success — hiển thị top-right và nội dung song ngữ verbatim | 1. Đăng nhập Org Portal.<br>2. Mở modal và gán thành công cặp hợp lệ (hồ sơ có SOS). | 1. Sau khi modal đóng, quan sát toast trên màn hình. | 1. Toast hiển thị ở vị trí top-right trên layout top-level (không phụ thuộc điểm vào — về danh sách thiết bị hay chi tiết bệnh nhân đều như nhau):<br>- Title song ngữ verbatim: "Đã gán thiết bị cho bệnh nhân. / Device assigned to the patient."<br>- Subtitle song ngữ verbatim: "Đang đồng bộ 3 số SOS xuống thiết bị. / Syncing the 3 SOS numbers to the device."<br>Toast auto-dismiss sau ~4s; có icon X để dismiss sớm. | P0 |
| TC_028 | Modal CRULE-013 (phiên hết hạn) — hiển thị verbatim song ngữ | 1. Đăng nhập Org Portal.<br>2. Mở modal và chọn cặp hợp lệ.<br>3. Mock invalidate phiên đăng nhập ở backend trước khi user bấm Xác nhận. | 1. Nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát modal phiên hết hạn. | 2. Modal toàn portal (CRULE-013) hiển thị:<br>- Title song ngữ: "Phiên đăng nhập đã hết hạn / Session expired".<br>- Nút song ngữ: "Đăng nhập lại / Log in again".<br>Modal gán đóng; không có toast hay banner thông báo khác. | P0 |
| TC_029 | Đóng modal qua icon X ở state default | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default, chưa chọn item hoặc đã chọn nhưng chưa submit. | 1. Nhấp vào icon "Đóng" (X) ở header. | 1. Modal đóng ngay; không tạo liên kết; không hiển thị toast; trang nền giữ nguyên trạng thái trước khi mở modal. | P0 |
| TC_030 | Đóng modal qua click backdrop ở state default | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Click vào vùng backdrop bên ngoài khung modal. | 1. Modal đóng ngay; không tạo liên kết; không hiển thị toast. | P1 |
| TC_031 | Đóng modal qua phím Esc ở state default (build tích hợp) | 1. Build tích hợp đã thật sự hiện thực Esc handler ở tầng Modal dùng chung.<br>2. Đăng nhập Org Portal, mở modal ở state default. | 1. Nhấn phím Esc trên bàn phím. | 1. Modal đóng ngay; không tạo liên kết; không hiển thị toast. Nếu test trên prototype mà Esc chưa render → KHÔNG log defect false-positive (Q3 Answered — verify trên build tích hợp). | P1 |
| TC_032 | Thao tác Tab và Shift + Tab trên modal | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default, selector đã ready. | 1. Nhấn Tab nhiều lần từ vị trí context block.<br>2. Nhấn Shift + Tab để quay ngược. | 1. Focus di chuyển theo thứ tự logic: selector → nút Huỷ → nút Xác nhận → icon X (focus trap không thoát ra khỏi modal); focus ring (focus indicator) hiển thị rõ trên mọi phần tử interactive.<br>2. Focus di chuyển ngược lại đúng thứ tự. | P2 |
| TC_033 | Phím Enter trên selector không submit nhầm form | 1. Đăng nhập Org Portal.<br>2. Mở modal, selector đã ready. | 1. Đưa focus vào selector.<br>2. Nhấn Enter khi đang mở dropdown và đã highlight 1 item.<br>3. Quan sát hành vi. | 3. Phím Enter chọn item đang highlight trong dropdown; KHÔNG gửi yêu cầu tạo liên kết (vì nút "Xác nhận gán" mới là trigger submit). | P2 |
| TC_034 | Nhấn phím F5 / Refresh khi modal đang mở | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default, đã chọn 1 item. | 1. Nhấn phím F5 (hoặc nút Refresh trình duyệt). | 1. Trang nền refresh; modal đóng; giá trị item đã chọn KHÔNG được lưu (theo CRULE-013-style — không lưu state đang nhập); không tạo liên kết. | P2 |
| TC_035 | Nhấn nút Back của trình duyệt khi modal đang mở | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Nhấn nút Back trình duyệt. | 1. Modal đóng và trình duyệt điều hướng về trang trước; KHÔNG tạo liên kết; KHÔNG submit. | P2 |
| TC_036 | Kiểm tra khi zoom in / zoom out trình duyệt | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Thực hiện zoom in 125%, 150%, 175%, 200%.<br>2. Thực hiện zoom out 90%, 75%, 67%. | 1. Layout modal không bị vỡ, không xuất hiện scroll bất thường ở viewport desktop ≥1280px; mọi text và nút vẫn đọc được và click được; không có text bị cắt.<br>2. Layout không bị vỡ, không xuất hiện overlap giữa các phần tử. | P2 |
| TC_037 | Tính nhất quán của message song ngữ trên toàn modal | 1. Đăng nhập Org Portal. | 1. Mở modal và lần lượt kích hoạt từng trạng thái: default, loading-candidates, empty, blocked-archived, submitting, các banner danger 5x-a/5x-b/5x-c, toast success, modal phiên hết hạn.<br>2. Đối chiếu mọi label, helper, banner, nút, toast với UC §3. | 2. Mọi text hiển thị đều ở dạng song ngữ "VN / EN" theo CRULE-010, đúng verbatim UC §3, không bị thiếu một trong hai phần, không bị paraphrase, không có text VN-only hay EN-only ở phần tử nào. | P0 |
| TC_038 | Kiểm tra UI modal so với design (font, color, spacing, border, icon) | 1. Đăng nhập Org Portal.<br>2. Mở modal ở viewport desktop ≥1280px. | 1. So sánh modal với prototype design `screens-device-assign.jsx` (lưu ý prototype còn delta song ngữ EN cho banner danger + 2 nút footer — chấp nhận theo UC §10). | 1. Modal hiển thị giống design về: vị trí header/body/footer; font family, kích thước, weight, line-height; màu nền, màu primary (`brand.primary.600 = #036AE5`), màu danger banner; padding/margin/spacing giữa các phần tử; border-radius khung modal; icon leading "link" trên nút Xác nhận; icon X header 16px; shadow modal; không có text bị chồng lấn hoặc lệch cột. | P1 |
| TC_039 | Layout responsive ở breakpoint desktop ≥1280px | 1. Đăng nhập Org Portal qua trình duyệt desktop ≥1280px. | 1. Mở modal và quan sát layout. | 1. Modal hiển thị căn giữa với chiều rộng cố định theo design; mọi nội dung hiển thị đầy đủ trong 1 cột; không có scroll bất thường; footer dính dưới cùng modal. | P1 |
| TC_040 | Layout responsive ở breakpoint laptop 1024px | 1. Đăng nhập Org Portal, set viewport 1024px. | 1. Mở modal và quan sát layout. | 1. Modal reflow phù hợp 1024px; không có horizontal scroll; mọi nội dung readable; nút "Huỷ / Cancel" và "Xác nhận gán / Confirm assignment" vẫn nhìn thấy và click được; banner song ngữ wrap đúng. | P1 |
| TC_041 | Layout responsive ở breakpoint tablet 768px | 1. Đăng nhập Org Portal, set viewport 768px. | 1. Mở modal và quan sát layout. | 1. Modal reflow phù hợp 768px (có thể full-width hoặc gần full-width tuỳ design); không bị overlap; mọi banner song ngữ wrap đầy đủ; selector vẫn click và search được bằng touch hoặc bàn phím ảo. | P1 |
| TC_042 | Layout responsive ở breakpoint mobile 375-414px | 1. Đăng nhập Org Portal, set viewport 375px và 414px. | 1. Mở modal và quan sát layout. | 1. Modal reflow single-column; mọi nội dung không bị clipped; không có horizontal scroll; nút footer xếp ngang hoặc xếp dọc theo design, vẫn click được; banner song ngữ wrap nhiều dòng đúng; address bar mobile show/hide không che footer. | P1 |
| TC_043 | Tap target tối thiểu 44px ở breakpoint touch | 1. Đăng nhập Org Portal trên trình duyệt touch (mobile/tablet emulator) hoặc viewport ≤768px. | 1. Đo kích thước tap target của icon X, nút Huỷ, nút Xác nhận, item trong dropdown selector. | 1. Mọi phần tử interactive có vùng tap ≥ 44px × 44px; không có 2 phần tử nằm quá gần gây tap nhầm. | P2 |
| TC_044 | Focus indicator visible khi navigate bằng bàn phím | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default. | 1. Lần lượt dùng Tab di chuyển focus qua từng phần tử interactive. | 1. Focus indicator (focus ring / outline) hiển thị rõ trên mọi phần tử nhận focus: selector, nút Huỷ, nút Xác nhận, icon X; tương phản đủ rõ trên nền modal. | P2 |
| TC_045 | Long Vietnamese labels không vỡ layout | 1. Đăng nhập Org Portal.<br>2. Mở modal với bệnh nhân/thiết bị có họ tên rất dài (ví dụ 80 ký tự) hoặc IMEI dài. | 1. Quan sát context block, dropdown item, banner song ngữ. | 1. Các label dài tự wrap nhiều dòng hoặc ellipsis có tooltip tuỳ design; không vỡ layout; không che banner SOS; không gây horizontal scroll; cặp song ngữ song không bị tách giữa các dòng. | P2 |
| TC_046 | Diacritics tiếng Việt render chính xác | 1. Đăng nhập Org Portal. | 1. Mở modal và quan sát mọi text tiếng Việt có dấu (Đ, ô, ơ, ư, ă, â, ê, ấ, ế, ố, ờ, ợ…) ở tiêu đề, banner, helper, placeholder, toast.<br>2. Trigger banner danger #3.1 và toast success để verify. | 1. Mọi diacritics hiển thị chính xác, không bị chuyển sang ASCII (không có "Sẵn sàng" → "San sang"), không có ô vuông `?`, không có mojibake (Ã©, Ä).<br>2. Banner danger #3.1 "Không thể gán thiết bị cho bệnh nhân đã ngưng theo dõi." và toast title "Đã gán thiết bị cho bệnh nhân." render đúng. | P0 |

### I.2. Kiểm tra FUNC của màn hình: Modal gán thiết bị cho bệnh nhân

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_047 | Mở modal từ chi tiết hồ sơ bệnh nhân (variant patient-first) — AC-01 | 1. Đăng nhập Org Portal bằng tài khoản Nhân viên tổ chức y tế.<br>2. Tenant có ít nhất 1 thiết bị "Sẵn sàng" trong kho.<br>3. Bệnh nhân P1 đang theo dõi (`active`) và chưa có thiết bị. | 1. Điều hướng đến chi tiết hồ sơ bệnh nhân P1.<br>2. Nhấp vào hành động "Gán thiết bị". | 2. Modal "Modal gán thiết bị cho bệnh nhân" (SCR-DEVICE-002) mở với:<br>- Context block hiển thị P1 (mã + tên + dob/gender + badge song ngữ "Đang theo dõi / Active").<br>- Selector "Thiết bị / Device" với placeholder song ngữ "Chọn thiết bị Sẵn sàng… / Select an available device…" và danh sách thiết bị "Sẵn sàng" của tenant.<br>- Variant tag song ngữ "Vào từ hồ sơ Bệnh nhân / From patient profile". | P0 |
| TC_048 | Mở modal từ danh sách thiết bị (variant device-first) — AC-02 | 1. Đăng nhập Org Portal.<br>2. Tenant có ít nhất 1 bệnh nhân đủ điều kiện.<br>3. Có thiết bị D1 ở trạng thái "Sẵn sàng" trong kho. | 1. Điều hướng đến SCR-DEVICE-001 (Danh sách thiết bị tổ chức).<br>2. Chọn thiết bị D1 và nhấp hành động "Gán". | 2. Modal mở với:<br>- Context block hiển thị D1 (mã + IMEI masked + badge song ngữ "Sẵn sàng / Available").<br>- Selector "Bệnh nhân / Patient" với placeholder song ngữ "Chọn bệnh nhân đang theo dõi… / Select an active patient…" và danh sách bệnh nhân đủ điều kiện.<br>- Variant tag song ngữ "Vào từ danh sách Thiết bị / From device list". | P0 |
| TC_049 | Gán thành công cặp hợp lệ, hồ sơ có SOS (variant patient-first) — AC-07 | 1. Đăng nhập Org Portal.<br>2. Mở modal từ chi tiết hồ sơ bệnh nhân P1 (đang theo dõi, chưa có thiết bị, hồ sơ đã có 3 số SOS). | 1. Mở selector "Thiết bị / Device" và chọn thiết bị D1 ("Sẵn sàng").<br>2. Nhấp "Xác nhận gán / Confirm assignment".<br>3. Đợi server phản hồi. | 3. Hệ thống thực hiện:<br>- Modal đóng **ngay** sau khi server lưu liên kết và ghi audit log xong.<br>- Hiển thị toast song ngữ top-right: title "Đã gán thiết bị cho bệnh nhân. / Device assigned to the patient." + subtitle "Đang đồng bộ 3 số SOS xuống thiết bị. / Syncing the 3 SOS numbers to the device."<br>- Trạng thái thiết bị D1 chuyển "Sẵn sàng" → "Đã gán" trên danh sách thiết bị tổ chức.<br>- Hồ sơ bệnh nhân P1 hiển thị thiết bị đang gán + trạng thái sync = `pending` (sẽ cập nhật thành `synced` hoặc `failed` sau khi đồng bộ async hoàn tất). | P0 |
| TC_050 | Gán thành công cặp hợp lệ, hồ sơ có SOS (variant device-first) — AC-07 | 1. Đăng nhập Org Portal.<br>2. Mở modal qua hành động "Gán" cho thiết bị D1 ("Sẵn sàng") trên SCR-DEVICE-001. | 1. Mở selector "Bệnh nhân / Patient" và chọn bệnh nhân P1 (active, chưa có thiết bị, hồ sơ có 3 số SOS).<br>2. Nhấp "Xác nhận gán / Confirm assignment".<br>3. Đợi server phản hồi. | 3. Modal đóng ngay; toast song ngữ hiển thị đúng nội dung TC_049; trạng thái thiết bị D1 chuyển sang "Đã gán"; hồ sơ P1 hiển thị thiết bị đang gán + sync = `pending`. Sau khi async hoàn tất, sync chuyển `synced` hoặc `failed`. | P0 |
| TC_051 | Gán thành công, hồ sơ chưa có SOS (Alt 6a) — AC-08 | 1. Đăng nhập Org Portal.<br>2. Mở modal từ chi tiết hồ sơ bệnh nhân P2 (active, chưa có thiết bị, hồ sơ chưa có số SOS nào). | 1. Chọn thiết bị D1 ("Sẵn sàng") trong selector.<br>2. Nhấp "Xác nhận gán / Confirm assignment".<br>3. Đợi server phản hồi. | 3. Hệ thống thực hiện:<br>- Modal đóng ngay.<br>- Toast song ngữ top-right: title "Đã gán thiết bị cho bệnh nhân. / Device assigned to the patient." + subtitle "Hồ sơ chưa có số SOS nên không có gì để đồng bộ. / The profile has no SOS numbers, so there is nothing to sync."<br>- Không có command `SOS` được sinh và gửi qua Gateway.<br>- Trạng thái sync trên hồ sơ P2 = "không có số SOS để đồng bộ".<br>- Trạng thái thiết bị D1 chuyển "Sẵn sàng" → "Đã gán". | P0 |
| TC_052 | Verify DB sau khi gán — bản ghi liên kết tồn tại với constraint 1-1 — AC-09 | 1. Quyền truy cập query DB cho QC.<br>2. Đã thực hiện thành công TC_049 hoặc TC_050. | 1. Query bảng liên kết thiết bị-bệnh nhân theo cặp (D1, P1).<br>2. Thử insert thêm bản ghi liên kết (D1, P3) qua API và (D2, P1) qua API. | 1. Tồn tại đúng 1 bản ghi liên kết (D1, P1) với tenant context khớp Org User; trường trạng thái thiết bị trong DB = "assigned to P1".<br>2. Cả 2 lần insert đều bị DB constraint từ chối (1 device — 1 patient, 1 patient — 1 device); message lỗi tham chiếu unique constraint. | P1 |
| TC_053 | Verify trạng thái thiết bị chuyển Sẵn sàng → Đã gán sau gán — AC-09 | 1. Đăng nhập Org Portal.<br>2. Đã thực hiện thành công TC_049 hoặc TC_050. | 1. Điều hướng về SCR-DEVICE-001 (Danh sách thiết bị tổ chức).<br>2. Tìm thiết bị D1 và kiểm tra cột trạng thái.<br>3. Áp filter "Đã gán" hoặc "assigned to <patient>". | 2. Thiết bị D1 hiển thị trạng thái "Đã gán" (Assigned) trên danh sách.<br>3. D1 xuất hiện trong filter "Đã gán"/"assigned to P1"; không còn xuất hiện trong filter "Sẵn sàng". | P0 |
| TC_054 | Verify audit log entry với payload nghiệp vụ — AC-09 + AC-21 | 1. Có công cụ truy vấn audit log store.<br>2. Đã thực hiện thành công TC_049. | 1. Query audit log theo run-id hoặc theo timestamp lúc gán.<br>2. Đếm số entry sinh ra cho thao tác này. | 1. Có entry audit log chứa các trường nghiệp vụ verbatim theo BR-006:<br>- user (Org User thực hiện).<br>- timestamp.<br>- tenant context.<br>- mã thiết bị (D1).<br>- mã bệnh nhân (P1).<br>- kết quả sync SOS (giá trị enum sau khi async hoàn tất hoặc `pending` tại thời điểm ghi).<br>2. Có 1 entry cho thao tác gán và 1 entry cho action đồng bộ SOS (theo Q17 Answered — cả thao tác gán + action SOS sync đều được ghi); retention 7 năm. Không assert schema kỹ thuật (IP/user-agent/định dạng) vì ngoài phạm vi UC. | P0 |
| TC_055 | Verify enum sync SOS = pending khi command đã gửi — AC-07 + AC-09 | 1. Đăng nhập Org Portal.<br>2. Đã thực hiện thành công TC_049 (hồ sơ có 3 số SOS).<br>3. Mock Gateway chậm phản hồi để giữ trạng thái pending lâu hơn. | 1. Ngay sau khi modal đóng, điều hướng vào chi tiết hồ sơ bệnh nhân P1.<br>2. Kiểm tra trường trạng thái sync SOS. | 2. Trạng thái sync SOS trên hồ sơ P1 = `pending`; enum cho phép chuyển tiếp sang `synced` hoặc `failed` (Q6 Resolved — enum chính thức `pending`/`synced`/`failed`/`chưa có thiết bị`); không hiển thị giá trị enum khác ngoài 4 giá trị này. | P1 |
| TC_056 | Verify enum sync SOS = "không có số SOS để đồng bộ" khi hồ sơ chưa có SOS — AC-08 | 1. Đăng nhập Org Portal.<br>2. Đã thực hiện thành công TC_051. | 1. Điều hướng vào chi tiết hồ sơ bệnh nhân P2.<br>2. Kiểm tra trường trạng thái sync SOS. | 2. Trạng thái sync SOS trên hồ sơ P2 = "không có số SOS để đồng bộ" (giá trị enum đặc biệt cho Alt 6a, không phải `pending`/`synced`/`failed`/`chưa có thiết bị`); không có command `SOS` nào đã được gửi xuống Gateway cho cặp (D1, P2). | P1 |
| TC_057 | Selector — chỉ hiển thị thiết bị `Sẵn sàng`, lọc `Đã gán` và `Faulty` | 1. Đăng nhập Org Portal bằng tài khoản tenant có kho thiết bị gồm: thiết bị "Sẵn sàng" (D1), "Đã gán" (D2), "Faulty" (D3).<br>2. Mở modal variant patient-first. | 1. Mở selector "Thiết bị / Device".<br>2. Cuộn xem toàn bộ danh sách. | 2. Selector chỉ hiển thị D1 ("Sẵn sàng"); D2 ("Đã gán") và D3 ("Faulty") không xuất hiện trong danh sách; thử search bằng mã D2 hoặc D3 → trả về "Không tìm thấy / No results". | P0 |
| TC_058 | Selector — chỉ hiển thị bệnh nhân `active` chưa có thiết bị, lọc `archived` và `có thiết bị` | 1. Đăng nhập Org Portal bằng tài khoản tenant có: P1 (active, chưa có thiết bị), P2 (archived), P3 (active, đã có thiết bị).<br>2. Mở modal variant device-first. | 1. Mở selector "Bệnh nhân / Patient".<br>2. Cuộn xem toàn bộ danh sách. | 2. Selector chỉ hiển thị P1; P2 (archived) và P3 (đã có thiết bị) không xuất hiện; thử search bằng mã P2 hoặc P3 → trả về "Không tìm thấy / No results". | P0 |
| TC_059 | Selector — chỉ hiển thị đối tượng cùng tenant (BR-004) | 1. Đăng nhập Org Portal bằng tài khoản tenant A.<br>2. Hệ thống cũng có dữ liệu của tenant B (thiết bị "Sẵn sàng" và bệnh nhân đủ điều kiện). | 1. Mở modal variant patient-first ở tenant A.<br>2. Mở selector "Thiết bị / Device" và search bằng mã thiết bị của tenant B. | 2. Selector không hiển thị bất kỳ thiết bị nào của tenant B; trả về "Không tìm thấy / No results" cho search bằng mã thiết bị của tenant B. | P0 |
| TC_060 | Selector — search theo mã thiết bị | 1. Đăng nhập Org Portal.<br>2. Mở modal variant patient-first, kho có nhiều thiết bị "Sẵn sàng". | 1. Mở selector.<br>2. Nhập một phần mã thiết bị tồn tại (ví dụ "D00012"). | 2. Selector lọc và hiển thị các thiết bị có mã chứa chuỗi search "D00012"; các thiết bị không khớp được ẩn. | P1 |
| TC_061 | Selector — search theo 4 số cuối IMEI | 1. Đăng nhập Org Portal.<br>2. Mở modal variant patient-first; biết trước 4 số cuối IMEI của 1 thiết bị "Sẵn sàng". | 1. Mở selector.<br>2. Nhập đúng 4 số cuối IMEI. | 2. Selector lọc và hiển thị thiết bị có IMEI khớp 4 số cuối; không lộ phần IMEI bị che. | P2 |
| TC_062 | Selector — search theo mã bệnh nhân | 1. Đăng nhập Org Portal.<br>2. Mở modal variant device-first. | 1. Mở selector "Bệnh nhân / Patient".<br>2. Nhập một phần mã bệnh nhân tồn tại. | 2. Selector lọc và hiển thị các bệnh nhân có mã chứa chuỗi search. | P1 |
| TC_063 | Selector — search theo họ tên bệnh nhân có dấu tiếng Việt | 1. Đăng nhập Org Portal.<br>2. Mở modal variant device-first.<br>3. Tenant có bệnh nhân với tên có dấu, ví dụ "Nguyễn Thị Hồng Vân". | 1. Mở selector.<br>2. Nhập chuỗi có dấu, ví dụ "Hồng Vân".<br>3. Xoá nội dung, nhập chuỗi không dấu "Hong Van". | 2. Selector lọc và hiển thị bệnh nhân "Nguyễn Thị Hồng Vân".<br>3. Nếu design hỗ trợ search không dấu thì cũng trả về kết quả; nếu không hỗ trợ thì hiển thị "Không tìm thấy / No results" — ghi nhận hành vi theo design hệ thống của `SelectSearchable`. | P2 |
| TC_064 | Selector — search case-insensitive | 1. Đăng nhập Org Portal.<br>2. Mở modal. | 1. Search trong selector bằng chuỗi viết hoa toàn bộ, viết thường toàn bộ, hỗn hợp hoa/thường — đều khớp 1 phần mã hoặc tên hợp lệ. | 1. Cả 3 trường hợp đều trả về cùng tập kết quả; không phân biệt hoa/thường. | P2 |
| TC_065 | Selector — search có khoảng trắng đầu/cuối (auto trim) | 1. Đăng nhập Org Portal.<br>2. Mở modal. | 1. Search trong selector bằng chuỗi có khoảng trắng ở đầu và cuối (ví dụ "   D00012   "). | 1. Hệ thống auto trim và trả về kết quả như khi search "D00012" (không bị "Không tìm thấy" do khoảng trắng). | P2 |
| TC_066 | Selector — reset giá trị khi đóng/mở lại modal | 1. Đăng nhập Org Portal.<br>2. Mở modal, chọn 1 item trong selector. | 1. Đóng modal qua nút "Huỷ / Cancel".<br>2. Mở lại modal ngay lập tức từ cùng điểm vào.<br>3. Kiểm tra giá trị selector. | 3. Giá trị selector reset về trống; nút "Xác nhận gán / Confirm assignment" trở lại disabled. | P1 |
| TC_067 | Client KHÔNG hiển thị inline error khi chưa chọn item — AC-06 | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default, selector ready. | 1. KHÔNG chọn item nào trong selector.<br>2. Quan sát trạng thái nút "Xác nhận gán" và phía dưới selector. | 1+2. Nút "Xác nhận gán / Confirm assignment" disabled (không gửi request).<br>Client KHÔNG hiển thị bất kỳ inline error nào dưới selector (theo CRULE-006 — form 1 field bắt buộc thì chỉ disable nút). | P0 |
| TC_068 | Server-side validation required — gửi payload thiếu mã thiết bị qua API trực tiếp — AC-06 | 1. Có công cụ gửi request API trực tiếp.<br>2. Có token Org User hợp lệ. | 1. Gửi POST tới endpoint tạo liên kết với payload thiếu mã thiết bị, chỉ có mã bệnh nhân hợp lệ.<br>2. Quan sát response. | 2. Server từ chối theo CRULE-014; response message song ngữ verbatim "Vui lòng chọn Thiết bị. / Please select a device."; không tạo bản ghi liên kết; không ghi audit log thành công cho thao tác này. | P1 |
| TC_069 | Server-side validation required — gửi payload thiếu mã bệnh nhân qua API trực tiếp — AC-06 | 1. Có công cụ gửi request API trực tiếp.<br>2. Có token Org User hợp lệ. | 1. Gửi POST tới endpoint tạo liên kết với payload thiếu mã bệnh nhân, chỉ có mã thiết bị "Sẵn sàng" hợp lệ.<br>2. Quan sát response. | 2. Server từ chối; response message song ngữ verbatim "Vui lòng chọn Bệnh nhân. / Please select a patient."; không tạo bản ghi liên kết. | P1 |
| TC_070 | Bệnh nhân archived ở variant patient-first (Ex 2x) — AC-05 | 1. Đăng nhập Org Portal.<br>2. Có bệnh nhân P_arc ở trạng thái "Đã ngưng theo dõi" (archived). | 1. Điều hướng đến chi tiết hồ sơ của P_arc.<br>2. Nhấp "Gán thiết bị".<br>3. Quan sát modal sau khi mở. | 3. Modal mở ở trạng thái blocked-archived:<br>- Banner danger #3.1 hiển thị verbatim song ngữ (xem TC_019).<br>- Nút "Xác nhận gán / Confirm assignment" disabled (không gửi được request).<br>- User được hướng dẫn khôi phục qua UC-PATIENT-006 trước khi thử lại. | P0 |
| TC_071 | Race condition — thiết bị bị gán đồng thời (Ex 5x-a) — AC-10 | 1. User A đang mở modal variant patient-first và đã chọn thiết bị D1.<br>2. User B của cùng tenant gán D1 cho bệnh nhân P2 thành công (trước khi A bấm Xác nhận). | 1. User A nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát modal của User A. | 2. Server recheck phát hiện D1 không còn "Sẵn sàng" → modal A hiển thị banner danger #3.2 song ngữ (xem TC_020); KHÔNG tạo liên kết mới; trạng thái thiết bị D1 = "Đã gán cho P2" giữ nguyên; selector cho phép User A chọn thiết bị khác (flow resume Step 3). | P0 |
| TC_072 | Race condition — bệnh nhân bị gán đồng thời (Ex 5x-b) — AC-11 | 1. User A đang mở modal variant device-first và đã chọn bệnh nhân P1.<br>2. User B của cùng tenant gán thiết bị khác cho P1 thành công (trước khi A bấm Xác nhận). | 1. User A nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát modal của User A. | 2. Server recheck phát hiện P1 đã có thiết bị → modal A hiển thị banner danger #3.3 verbatim song ngữ giữ "liên kết / link" (xem TC_021); KHÔNG tạo liên kết mới; user A được hướng dẫn kiểm tra lại tình trạng thiết bị của P1. | P0 |
| TC_073 | System error khi lưu liên kết DB (Ex 5x-c) — AC-12 | 1. Đăng nhập Org Portal.<br>2. Mock DB connection drop khi server thực hiện lưu liên kết ở Step 5.<br>3. Mở modal và chọn cặp hợp lệ. | 1. Nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát modal sau khi server lỗi. | 2. Modal vẫn mở; banner danger #3.4 verbatim song ngữ hiển thị (xem TC_022); KHÔNG tạo bản ghi liên kết; trạng thái thiết bị D1 giữ nguyên "Sẵn sàng"; trạng thái hồ sơ bệnh nhân giữ nguyên; không có audit log entry "thành công" cho thao tác này (rollback toàn bộ — không tạo liên kết một phần). | P0 |
| TC_074 | System error khi ghi audit log (Ex 5x-c) — AC-12 | 1. Đăng nhập Org Portal.<br>2. Mock audit store fail khi server thực hiện ghi audit log ở Step 7.<br>3. Mở modal và chọn cặp hợp lệ. | 1. Nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát modal sau khi audit log fail. | 2. Theo BR-006 (audit log phải được ghi trước khi công nhận thành công): server rollback toàn bộ; banner danger #3.4 verbatim song ngữ hiển thị; KHÔNG tồn tại bản ghi liên kết một phần; trạng thái thiết bị, hồ sơ bệnh nhân, audit store giữ nguyên trước khi thao tác. | P0 |
| TC_075 | SOS sync fail sau khi liên kết đã commit (Ex 6x) — AC-13 | 1. Đăng nhập Org Portal.<br>2. Mock Gateway / thiết bị không phản hồi command `SOS` (timeout) sau khi server đã lưu liên kết và ghi audit log thành công.<br>3. Mở modal và chọn cặp hợp lệ với hồ sơ có 3 số SOS. | 1. Nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát modal, toast, và hồ sơ bệnh nhân sau khi async sync hoàn tất với kết quả failed. | 2. Hệ thống thực hiện:<br>- Liên kết được giữ (không rollback).<br>- Modal đóng **ngay** ở bước 8 (không chờ kết quả async).<br>- Toast success bình thường hiển thị verbatim (như TC_027): title + subtitle "Đang đồng bộ 3 số SOS xuống thiết bị. / Syncing the 3 SOS numbers to the device.".<br>- **KHÔNG có cảnh báo riêng** trên modal hay toast warning thêm (Q5 Resolved — modal đã đóng).<br>- Trạng thái sync SOS trên hồ sơ bệnh nhân chuyển sang `failed`.<br>- Audit log vẫn được ghi cho cả thao tác gán và action đồng bộ SOS. | P0 |
| TC_076 | Retry path thủ công qua UC-PATIENT-004 sau khi sync fail — AC-13 | 1. Đã thực hiện thành công TC_075 (trạng thái sync = `failed` trên hồ sơ).<br>2. Đăng nhập Org Portal. | 1. Điều hướng đến chi tiết hồ sơ bệnh nhân P1.<br>2. Thực hiện thao tác cập nhật / đồng bộ lại 3 số SOS thông qua UC-PATIENT-004.<br>3. Quan sát trạng thái sync sau khi server reprocess. | 3. Hệ thống KHÔNG có cơ chế auto-retry server (Q8 Resolved); user phải đồng bộ lại thủ công qua UC-PATIENT-004; sau khi UC-PATIENT-004 chạy thành công, trạng thái sync trên hồ sơ P1 chuyển từ `failed` → `pending` → `synced` (hoặc `failed` nếu Gateway vẫn fail). | P1 |
| TC_077 | Cross-tenant attempt qua API trực tiếp — AC-14 + AC-20 | 1. Có tài khoản Org User tenant A với token hợp lệ.<br>2. Có công cụ gửi request API trực tiếp.<br>3. Biết mã thiết bị/bệnh nhân của tenant B (qua deep link giả mạo hoặc payload chế tạo). | 1. Dùng token tenant A gửi POST tạo liên kết với mã thiết bị thuộc tenant B (cố ý).<br>2. Lặp lại bước 1 với mã bệnh nhân thuộc tenant B. | 1. Server validate theo CRULE-014 + BR-004 và từ chối; response message song ngữ verbatim "Không tìm thấy thiết bị. / Device not found." (không lộ tồn tại của thiết bị cross-tenant); KHÔNG tạo bản ghi liên kết.<br>2. Response message song ngữ verbatim "Không tìm thấy bệnh nhân. / Patient not found."; KHÔNG tạo bản ghi liên kết.<br>UI không gặp tình huống này vì đã lọc theo tổ chức — không có UI fallback path. | P0 |
| TC_078 | Phiên hết hạn giữa Step 4-5 (Ex 4y) — AC-15 | 1. Đăng nhập Org Portal.<br>2. Mở modal, chọn cặp hợp lệ.<br>3. Mock invalidate phiên đăng nhập ở backend ngay trước khi user bấm Xác nhận (hoặc set thời gian timeout phiên ngắn để tự hết hạn khi user đang chọn). | 1. Nhấp "Xác nhận gán / Confirm assignment".<br>2. Quan sát modal phiên hết hạn (CRULE-013). | 2. Thao tác bị block; modal toàn portal hiển thị (CRULE-013):<br>- Title song ngữ "Phiên đăng nhập đã hết hạn / Session expired".<br>- Nút song ngữ "Đăng nhập lại / Log in again".<br>Dữ liệu đang nhập của modal gán (item đã chọn) **không được lưu** (Q14 Resolved). | P0 |
| TC_079 | Bấm "Đăng nhập lại" từ modal phiên hết hạn — AC-15 | 1. Đã thực hiện đến trạng thái modal phiên hết hạn (xem TC_078). | 1. Nhấp nút "Đăng nhập lại / Log in again". | 1. Hệ thống điều hướng user về luồng UC-ORGUSER-001 (Đăng nhập Org Portal); sau khi đăng nhập lại thành công, KHÔNG tự mở lại modal gán; KHÔNG khôi phục cặp item đã chọn trước đó. | P1 |
| TC_080 | Empty state — nút Xác nhận disabled + tooltip — AC-04 | 1. Đăng nhập Org Portal bằng tài khoản tenant không có thiết bị "Sẵn sàng" trong kho.<br>2. Có bệnh nhân P1 đủ điều kiện. | 1. Mở modal từ chi tiết hồ sơ P1.<br>2. Hover lên nút "Xác nhận gán / Confirm assignment". | 2. Selector hiển thị empty message verbatim song ngữ (xem TC_004); nút "Xác nhận gán / Confirm assignment" disabled; tooltip hiển thị verbatim song ngữ "Chưa có thiết bị Sẵn sàng nào để gán. / No available device to assign yet." (tuỳ pickerKind). | P1 |
| TC_081 | Duplicate submit prevention — double-click nút Xác nhận | 1. Đăng nhập Org Portal.<br>2. Mở modal và chọn cặp hợp lệ.<br>3. Cấu hình network throttle để API tạo liên kết trả về chậm. | 1. Click nhanh 2-3 lần liên tiếp vào nút "Xác nhận gán / Confirm assignment".<br>2. Quan sát log network và kết quả. | 2. Chỉ 1 request tạo liên kết được gửi đi (debounce / button-loading bảo vệ); chỉ 1 bản ghi liên kết được tạo trong DB; chỉ 1 entry audit log "gán" được sinh; toast hiển thị 1 lần. | P0 |
| TC_082 | Duplicate submit prevention — phím Enter nhiều lần khi đang submit | 1. Đăng nhập Org Portal.<br>2. Mở modal, chọn cặp hợp lệ, network chậm. | 1. Đưa focus vào nút "Xác nhận gán" và nhấn Enter nhiều lần liên tiếp. | 1. Chỉ 1 request được gửi đi; các lần Enter sau bị nuốt vì nút đã chuyển trạng thái disabled+spinner. | P1 |
| TC_083 | Đóng modal qua 3 kênh ở build tích hợp (state default) — AC-16 | 1. Build tích hợp đã hiện thực Esc handler ở tầng Modal dùng chung.<br>2. Đăng nhập Org Portal, mở modal ở state default. | 1. (a) Mở modal lần 1 và đóng bằng icon X.<br>2. (b) Mở modal lần 2 và đóng bằng click backdrop.<br>3. (c) Mở modal lần 3 và đóng bằng phím Esc. | 1+2+3. Cả 3 kênh đều đóng modal ngay; KHÔNG tạo liên kết ở lần nào; KHÔNG hiển thị toast; trang nền giữ nguyên. Nếu test trên prototype mà (c) chưa render Esc → KHÔNG log defect false-positive (Q3 Answered). | P0 |
| TC_084 | Chặn đóng modal qua 3 kênh khi đang submitting — AC-17 | 1. Đăng nhập Org Portal.<br>2. Mở modal, chọn cặp hợp lệ, network chậm.<br>3. Nhấp "Xác nhận gán" → state=submitting. | 1. Trong lúc state=submitting, thử (a) click icon X, (b) click backdrop, (c) nhấn Esc. | 1. Cả 3 kênh bị chặn; modal vẫn mở; spinner trên nút Xác nhận vẫn chạy; không có thay đổi trạng thái nào cho đến khi server phản hồi (success → đóng modal + toast / error 5x-c → giữ modal + banner danger). | P0 |
| TC_085 | Nút Huỷ — đóng modal không tạo liên kết | 1. Đăng nhập Org Portal.<br>2. Mở modal ở state default, đã chọn 1 item trong selector. | 1. Nhấp nút "Huỷ / Cancel". | 1. Modal đóng ngay; không tạo liên kết; không hiển thị toast; trạng thái thiết bị, hồ sơ bệnh nhân, audit log đều giữ nguyên. | P1 |
| TC_086 | Nút Huỷ disabled khi state=submitting | 1. Đăng nhập Org Portal.<br>2. Mở modal, chọn cặp hợp lệ, network chậm.<br>3. Nhấp Xác nhận → state=submitting. | 1. Trong lúc submitting, thử click nút "Huỷ / Cancel". | 1. Nút "Huỷ / Cancel" disabled; click không có hiệu lực; modal không đóng cho đến khi server phản hồi. | P1 |
| TC_087 | Downstream UC-DEVICE-003 — danh sách thiết bị cập nhật trạng thái sau gán | 1. Đăng nhập Org Portal.<br>2. Đã gán thành công cặp (D1, P1) qua TC_049 hoặc TC_050. | 1. Điều hướng đến SCR-DEVICE-001 (Danh sách thiết bị tổ chức).<br>2. Re-query / re-load danh sách.<br>3. Tìm thiết bị D1. | 3. D1 hiển thị trạng thái "Đã gán" trên cột trạng thái; xuất hiện trong filter "assigned to P1"; không xuất hiện trong filter "Sẵn sàng". | P0 |
| TC_088 | Downstream MOD-ORG-PATIENT — hồ sơ bệnh nhân hiển thị thiết bị đang gán + sync status | 1. Đã gán thành công cặp (D1, P1) qua TC_049. | 1. Điều hướng đến chi tiết hồ sơ bệnh nhân P1. | 1. Hồ sơ P1 hiển thị mã thiết bị đang gán (D1); hiển thị trạng thái sync SOS theo enum chính thức `pending`/`synced`/`failed`/`chưa có thiết bị` (Q6 Resolved); giá trị enum khớp với giá trị mà UC-DEVICE-001 đã ghi cho từng trường hợp (pending sau khi gán, "không có số SOS để đồng bộ" nếu Alt 6a, `failed` nếu Ex 6x). | P0 |
| TC_089 | Verify Gateway binding mới qua DEVPWDCHECKBIND sau gán — AC-09 | 1. Có thiết bị sandbox kết nối Gateway TCP V3.2.<br>2. Đã gán thành công cặp (D_sandbox, P_test). | 1. Trigger thiết bị D_sandbox reconnect tới Gateway.<br>2. Quan sát log Gateway và phản hồi DEVPWDCHECKBIND. | 2. Gateway xác thực binding mới cho D_sandbox ↔ P_test; thiết bị reconnect thành công; log Gateway phản ánh binding tồn tại với tenant context khớp; nếu unbind / gán lại bệnh nhân khác, binding cập nhật theo. | P1 |
| TC_090 | Hiển thị song ngữ toàn modal — kiểm tra tổng hợp (CRULE-010) — AC-18 | 1. Đăng nhập Org Portal. | 1. Mở modal và lần lượt verify mọi label/banner/toast/nút/helper/placeholder/tooltip có cấu trúc "VN / EN" verbatim theo UC §3 (đã canonical).<br>2. Đối chiếu phần EN với UC §3 (verbatim) — KHÔNG paraphrase. | 2. Mọi text hiển thị đều có cặp song ngữ VN/EN verbatim đúng UC §3; không có phần tử nào VN-only hoặc EN-only; thứ tự "VN / EN" được giữ nhất quán. | P0 |
| TC_091 | Verify integration end-to-end (variant patient-first, có SOS) — Happy path tổng hợp | 1. Setup test data: tenant T1 với thiết bị D1 "Sẵn sàng" và bệnh nhân P1 active có 3 số SOS.<br>2. Đăng nhập Org Portal bằng Org User của T1. | 1. Mở chi tiết hồ sơ P1.<br>2. Bấm "Gán thiết bị".<br>3. Chọn D1 trong selector.<br>4. Bấm "Xác nhận gán / Confirm assignment".<br>5. Sau khi modal đóng, quay lại danh sách thiết bị + chi tiết bệnh nhân + audit log. | 5. Hệ thống đạt post-condition đầy đủ:<br>- Bản ghi liên kết (D1, P1) lưu trong DB với tenant T1 context.<br>- Trạng thái thiết bị D1 = "Đã gán" trên danh sách thiết bị tổ chức.<br>- Trạng thái sync SOS trên hồ sơ P1 = `pending` (sau đó async chuyển sang `synced`).<br>- Audit log entry với user T1 Org User + timestamp + tenant T1 + mã D1 + mã P1 + kết quả sync SOS.<br>- Toast thành công hiển thị + modal đóng ngay sau Step 7.<br>- Gateway đã nhận binding mới (verify ở TC_089 nếu có sandbox). | P0 |
| TC_092 | Verify integration end-to-end (variant device-first, không SOS) — Alt 6a tổng hợp | 1. Setup test data: tenant T1 với thiết bị D2 "Sẵn sàng" và bệnh nhân P2 active chưa có 3 số SOS.<br>2. Đăng nhập Org Portal bằng Org User của T1. | 1. Mở danh sách thiết bị T1.<br>2. Chọn D2 và bấm "Gán".<br>3. Chọn P2 trong selector.<br>4. Bấm "Xác nhận gán".<br>5. Sau khi modal đóng, quay lại chi tiết bệnh nhân + audit log. | 5. Post-condition Alt 6a:<br>- Bản ghi liên kết (D2, P2) lưu trong DB với tenant T1.<br>- Trạng thái thiết bị D2 = "Đã gán".<br>- Trạng thái sync SOS trên hồ sơ P2 = "không có số SOS để đồng bộ".<br>- KHÔNG có command `SOS` gửi xuống Gateway.<br>- Audit log entry tạo bình thường (Q17: payload nghiệp vụ).<br>- Toast: title + subtitle "Hồ sơ chưa có số SOS nên không có gì để đồng bộ. / The profile has no SOS numbers, so there is nothing to sync." | P0 |
| TC_093 | Cycle UC-DEVICE-001 → UC-DEVICE-002 → UC-DEVICE-001: gán lại sau thu hồi | 1. Đã thực hiện thành công TC_049 (gán D1 cho P1).<br>2. UC-DEVICE-002 (thu hồi) đã sẵn sàng. | 1. Thu hồi liên kết (D1, P1) qua UC-DEVICE-002.<br>2. Sau khi thu hồi, mở modal UC-DEVICE-001 và gán lại D1 cho P3 khác. | 1. Sau thu hồi: trạng thái D1 chuyển "Đã gán" → "Sẵn sàng"; trạng thái sync SOS trên hồ sơ P1 chuyển sang `chưa có thiết bị`; Gateway gỡ binding khỏi map.<br>2. Modal UC-DEVICE-001 hiển thị D1 lại trong danh sách selector; gán D1 cho P3 thành công như TC_049; tạo bản ghi liên kết (D1, P3) mới. | P1 |
| TC_094 | Sync SOS chạy async — modal đóng ngay không chờ kết quả Gateway | 1. Đăng nhập Org Portal.<br>2. Mock Gateway phản hồi rất chậm (>10s) cho command `SOS`.<br>3. Mở modal và chọn cặp hợp lệ với hồ sơ có 3 số SOS. | 1. Nhấp "Xác nhận gán / Confirm assignment".<br>2. Đo thời gian từ lúc bấm Xác nhận đến lúc modal đóng. | 2. Modal đóng ngay sau khi server commit liên kết và ghi audit log (Step 5 + Step 7), KHÔNG chờ Gateway phản hồi command `SOS`; thời gian đóng modal ngắn hơn nhiều so với thời gian Gateway phản hồi (chứng minh async); toast hiển thị ngay với subtitle "Đang đồng bộ 3 số SOS xuống thiết bị. / Syncing the 3 SOS numbers to the device."; trạng thái sync trên hồ sơ = `pending`. | P1 |
| TC_095 | DB-level uniqueness của liên kết (concurrency) | 1. 2 user A và B cùng tenant đang mở modal device-first cho cùng thiết bị D1.<br>2. User A đã chọn P1, User B đã chọn P2. | 1. Cả A và B cùng bấm "Xác nhận gán / Confirm assignment" gần như đồng thời.<br>2. Quan sát kết quả của cả 2 modal. | 2. Đúng 1 user nhận response thành công (modal đóng + toast); user còn lại nhận banner danger #3.2 (concurrent-device); chỉ 1 bản ghi liên kết tồn tại trong DB cho D1; DB constraint 1-1 device-patient được bảo vệ ngay cả khi server recheck lọt qua race. | P1 |

