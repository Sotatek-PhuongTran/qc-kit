---
UC: <UC-ID>
Environment: <DEV | QA/Staging | UAT>   # khớp 1 dòng "<ENV> - API" trong project-config §3
Generated by: qc-api-auto-generate
---
<!-- File SỐNG — 1 file/UC, KHÔNG ngày, KHÔNG version, sửa tại chỗ. Giá trị QC đã điền không bị ghi đè.
     Contract: .claude/config/api-shared/api-testdata-contract.md (delta trên data-and-secrets.md).
     KHÔNG BAO GIỜ chứa mật khẩu/token — accounts resolve theo role lúc chạy; token trong .env. -->

## Accounts
<!-- Key = role key mà ctxFor() dùng; Role khớp đúng 1 dòng project-config §4 -->
| Key | Role (project-config §4) |
|---|---|
| adminUser | Admin - Admin portal |

## Variables
<!-- Value là JSON fragment 1 dòng trong backtick cho payload; giá trị EP/BVA lấy từ api-audited §3 -->
| TC | Variable | Value | Note |
|---|---|---|---|
| TC_API_001 | payloadHopLe | `{"email":"user@org.vn","name":"Nguyen Van A","role":"USER"}` | payload đầy đủ hợp lệ |
| TC_API_002 | payloadThieuEmail | `{"name":"Nguyen Van A"}` | thiếu email — validate bắt buộc |
| TC_API_003 | emailMaxLenPlus1 | `a…a@test.vn` (256 ký tự) | biên trên +1 từ §3 |

## Preconditions
<!-- Schema 6 cột dùng chung với nhánh UI (data-and-secrets.md) — qc-auto-run pre-flight đọc bảng này -->
| Key | Required state | Seed channel | TCs | Check (pre-flight) | Confirmed |
|---|---|---|---|---|---|
| suspendedUser | một tài khoản user đã bị ngưng hoạt động, email cố định dành riêng cho test | api: services/users.service.ts#suspendUser | TC_API_015 | api: GET /api/v1/users?email={seed} → expect status=SUSPENDED | — |

## Config
<!-- Giá trị dùng chung không bí mật (vd hộp thư test). Bí mật → .env -->
| Key | Value |
|---|---|
