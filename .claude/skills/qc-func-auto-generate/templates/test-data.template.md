---
UC: <UC-ID>
Environment: QA/Staging          # which project-config §3 row gives the base URL (never PROD)
Generated by: qc-func-auto-generate
---

# Test Data — <UC-ID>

> ONE living file per UC (`<UC-ID>_testdata.md` — no date/version), edited IN-PLACE; history = git.
> Edit values here; the JSON fixture is rebuilt automatically by qc-auto-run before every run
> (manual rebuild: `npm run build:testdata -- <UC-ID>`). The JSON is a gitignored build output.
> Passwords are NOT stored here — credentials are read at runtime from `project-config §4` by role.

## Config

| Key | Value | Note |
|---|---|---|
| testInbox | qa@inbox.mailtrap.io | test inbox (email-testing tool) for dynamic email retrieval |

> Shared non-secret values. Secrets (API tokens, passwords, DB connections) go in `.env`, never here.

## Accounts

| Key | Role (project-config §4) |
|---|---|
| activeOrgUser | Standard User |

## Variables

| TC | Variable | Value | Note |
|---|---|---|---|
| TC_021 | validEmail | user@org.vn | email khớp tài khoản active |
| TC_022 | unknownEmail | notexist@org.vn | email không khớp tài khoản nào |
| TC_047 | validPassword | Abcdef1@ | mật khẩu thoả 5 quy tắc |
| TC_047 | mismatchedConfirm | Abcdef1@x | xác nhận khác mật khẩu mới |

## Preconditions

> One row per required data-state. Schema + field rules: skill `references/data-and-secrets.md`.
> `Seed channel`: `ui:<flow>#<fn>` / `api:<endpoint>` / `db:<script>` = seeded automatically by the suite; `manual` = the USER creates the state BEFORE running — `qc-auto-run` Blocks the dependent TCs until the Check passes (or Confirmed is filled when no check exists).

| Key | Required state | Seed channel | TCs | Check (pre-flight) | Confirmed |
|---|---|---|---|---|---|
| activeOrgUserAccount | một tài khoản Org User đang hoạt động | ui: setup.flows.ts#ensureActiveOrgUser | TC_033, TC_039 | none | — |
| suspendedOrgUser | một tài khoản Org User đã bị ngưng hoạt động, email dành riêng cho test | manual | TC_059 | api: GET /admin/users?email={seed} → expect status=SUSPENDED | — |
| expiredResetLink | một liên kết đặt lại mật khẩu đã hết hạn (quá 60 phút) | manual | TC_105 | none | — |
