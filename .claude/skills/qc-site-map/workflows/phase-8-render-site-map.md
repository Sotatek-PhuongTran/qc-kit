# Phase 8 - Render Site Map

## Goal

Merge checkpoints into the final `qc-site-map.md` using the Vietnamese template.

## Steps

1. Read `templates/qc-site-map-template.vi.md`.
2. Read checkpoints from Phases 1-7.
3. Render a compact Vietnamese `qc-site-map.md`.
4. Preserve QC Lead-reviewed content in Update mode when still valid.
5. Mark changed/deprecated/not-found items clearly instead of deleting silently.
6. Write the file to the resolved `qc-site-map.md` path.

## Rules

- Keep output screen-first.
- Use tables and short notes.
- Do not copy detailed specs or API schemas.
- Do not include test cases or scenarios.

## Output checkpoint

Write:

```text
process-logging/qc-site-map/08_site_map_rendered.md
```

Include final path, render summary, and any write risks.
